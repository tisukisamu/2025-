# 招聘管理系统前端文档

## 1. 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue 3 | 3.4.21 | 前端框架（Composition API） |
| Vite | 5.2.0 | 构建工具 |
| Ant Design Vue | 4.1.2 | UI组件库 |
| UnoCSS | 66.6.0 | 原子化CSS引擎 |
| Pinia | - | 状态管理 |
| Vue Router | 4.x | 路由管理 |
| Axios | - | HTTP客户端 |
| TypeScript | - | 类型支持 |

---

## 2. 工程结构

```
frontend/src
├── api/                           # API接口层
│   ├── request.ts                 # Axios封装、拦截器
│   ├── auth.ts                    # 认证接口
│   ├── user.ts                    # 用户接口
│   ├── company.ts                 # 企业接口
│   ├── job.ts                     # 职位接口
│   ├── resume.ts                  # 简历接口
│   ├── application.ts             # 投递接口
│   ├── interview.ts               # 面试接口
│   ├── announcement.ts            # 公告接口
│   ├── message.ts                 # 消息接口
│   ├── talent.ts                  # 人才库接口
│   ├── evaluation.ts              # 评价接口
│   ├── complaint.ts               # 投诉建议接口
│   ├── statistics.ts              # 统计接口
│   ├── admin.ts                   # 管理端接口
│   ├── favorite.ts                # 收藏接口
│   ├── file.ts                    # 文件上传接口
│   └── index.ts                   # 统一导出
│
├── components/                    # 公共组件
│   └── MainLayout.vue             # 主布局组件
│
├── router/                        # 路由配置
│   └── index.js                   # 路由定义、导航守卫
│
├── stores/                        # 状态管理
│   └── user.js                    # 用户状态
│
├── types/                         # TypeScript类型定义
│   ├── user.ts                    # 用户相关类型
│   ├── company.ts                 # 企业相关类型
│   ├── job.ts                     # 职位相关类型
│   ├── resume.ts                  # 简历相关类型
│   ├── application.ts             # 投递相关类型
│   ├── interview.ts               # 面试相关类型
│   ├── announcement.ts            # 公告相关类型
│   ├── message.ts                 # 消息相关类型
│   ├── talent.ts                  # 人才库相关类型
│   ├── evaluation.ts              # 评价相关类型
│   ├── complaint.ts               # 投诉相关类型
│   ├── common.ts                  # 通用类型
│   └── index.ts                   # 类型统一导出
│
├── views/                         # 页面视图
│   ├── Home.vue                   # 首页
│   ├── Profile.vue                # 个人中心
│   ├── About.vue                  # 关于页面
│   ├── NotFound.vue               # 404页面
│   ├── Users.vue                  # 用户列表
│   ├── auth/                      # 认证页面
│   │   ├── Login.vue              # 登录页
│   │   └── Register.vue           # 注册页
│   ├── jobs/                      # 职位页面
│   │   └── JobList.vue            # 职位列表
│   ├── resumes/                   # 简历页面
│   │   └── ResumeList.vue         # 简历管理
│   ├── applications/              # 投递页面
│   │   └── ApplicationList.vue    # 投递记录
│   ├── company/                   # 企业页面
│   │   ├── JobManage.vue          # 职位管理
│   │   └── ResumeReceived.vue     # 收到的简历
│   └── admin/                     # 管理页面
│       ├── Dashboard.vue          # 仪表盘
│       ├── UserManage.vue         # 用户管理
│       └── CompanyAudit.vue       # 企业审核
│
├── App.vue                        # 根组件
├── main.js                        # 入口文件
├── style.css                      # 全局样式
└── vite.config.js                 # Vite配置
```

---

## 3. 页面路由

### 3.1 路由配置

| 路径 | 组件 | 说明 | 权限 |
|------|------|------|------|
| `/` | Home | 首页 | 公开 |
| `/login` | Login | 登录页 | 公开 |
| `/register` | Register | 注册页 | 公开 |
| `/jobs` | JobList | 职位列表 | 公开 |
| `/jobs/:id` | JobDetail | 职位详情 | 公开 |
| `/about` | About | 关于页面 | 公开 |
| `/profile` | Profile | 个人中心 | 需登录 |
| `/resumes` | ResumeList | 简历管理 | 需登录 |
| `/applications` | ApplicationList | 投递记录 | 需登录 |
| `/company/jobs` | JobManage | 职位管理 | 企业用户 |
| `/company/resumes` | ResumeReceived | 收到的简历 | 企业用户 |
| `/admin` | Dashboard | 管理仪表盘 | ADMIN |
| `/admin/users` | UserManage | 用户管理 | ADMIN |
| `/admin/companies` | CompanyAudit | 企业审核 | ADMIN |
| `/:pathMatch(.*)*` | NotFound | 404页面 | 公开 |

### 3.2 路由守卫

```javascript
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  const isLoggedIn = userStore.isLoggedIn
  const userRole = userStore.userRole
  
  // 需要登录的页面
  if (to.meta.requiresAuth && !isLoggedIn) {
    next('/login')
    return
  }
  
  // 需要特定角色的页面
  if (to.meta.roles && !to.meta.roles.includes(userRole)) {
    next('/')
    return
  }
  
  next()
})
```

---

## 4. 状态管理

### 4.1 用户状态（user.js）

```javascript
export const useUserStore = defineStore('user', {
  state: () => ({
    user: null,
    token: localStorage.getItem('token') || null
  }),
  
  getters: {
    isLoggedIn: (state) => !!state.token,
    userRole: (state) => state.user?.role || null,
    isAdmin: (state) => state.user?.role === 'ADMIN',
    isCompany: (state) => state.user?.role === 'COMPANY'
  },
  
  actions: {
    async loginAction(credentials) { ... },
    async registerAction(userData) { ... },
    async fetchCurrentUser() { ... },
    logout() { ... }
  }
})
```

---

## 5. API封装

### 5.1 Axios实例配置

```typescript
const request = axios.create({
  baseURL: '/api',
  timeout: 10000
})

// 请求拦截器：自动添加Token
request.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器：统一错误处理
request.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)
```

### 5.2 API调用示例

```typescript
// 职位搜索
export const getJobs = (params?: JobQueryParams): Promise<ApiResponse<Job[]>> => {
  return get('/jobs', { params })
}

// 投递简历
export const applyJob = (data: ApplicationDTO): Promise<ApiResponse<Application>> => {
  return post('/applications', data)
}
```

---

## 6. 组件设计

### 6.1 主布局（MainLayout.vue）

**功能：**
- 顶部导航栏（Logo、菜单、用户信息）
- 侧边栏（根据角色显示不同菜单）
- 内容区域
- 消息通知弹窗

**菜单配置：**
```javascript
const menuItems = computed(() => {
  if (userStore.isAdmin) {
    return adminMenuItems
  } else if (userStore.isCompany) {
    return companyMenuItems
  }
  return userMenuItems
})
```

### 6.2 页面组件规范

```vue
<template>
  <div class="page-container">
    <!-- 页面头部 -->
    <div class="page-header">...</div>
    
    <!-- 内容区域 -->
    <div class="page-content">...</div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
// 组件逻辑
</script>

<style scoped>
/* 组件样式 */
</style>
```

---

## 7. 页面功能说明

### 7.1 首页（Home.vue）

**功能：**
- Hero区域：搜索框、热门关键词
- 统计数据展示
- 热门职位推荐
- 功能特性介绍
- 最新公告
- CTA行动召唤

### 7.2 登录页（Login.vue）

**功能：**
- 用户名/密码登录
- 记住登录状态
- 社交登录入口（微信、QQ）
- 跳转注册页

### 7.3 注册页（Register.vue）

**功能：**
- 用户注册表单
- 账户类型选择（求职者/企业）
- 用户协议确认

### 7.4 职位列表（JobList.vue）

**功能：**
- 职位搜索（关键词、城市）
- 筛选条件（经验、学历、薪资）
- 职位卡片列表
- 收藏功能
- 分页

### 7.5 简历管理（ResumeList.vue）

**功能：**
- 简历列表
- 创建/编辑简历
- 删除简历
- 简历预览

### 7.6 投递记录（ApplicationList.vue）

**功能：**
- 投递状态跟踪
- 状态筛选
- 撤回投递

### 7.7 职位管理（JobManage.vue）- 企业端

**功能：**
- 发布职位
- 编辑职位
- 关闭/删除职位
- 职位状态管理

### 7.8 收到的简历（ResumeReceived.vue）- 企业端

**功能：**
- 简历列表
- 状态筛选
- 安排面试
- 加入人才库

### 7.9 管理仪表盘（Dashboard.vue）

**功能：**
- 数据统计卡片
- 图表展示
- 最新动态

### 7.10 用户管理（UserManage.vue）

**功能：**
- 用户列表
- 状态管理（启用/禁用）
- 角色管理
- 用户详情

### 7.11 企业审核（CompanyAudit.vue）

**功能：**
- 待审核企业列表
- 审核通过/拒绝
- 企业详情查看

---

## 8. 样式规范

### 8.1 UnoCSS配置

```javascript
// uno.config.js
export default defineConfig({
  theme: {
    colors: {
      primary: '#667eea',
      secondary: '#764ba2'
    }
  }
})
```

### 8.2 主题色

| 名称 | 色值 | 用途 |
|------|------|------|
| 主色 | `#667eea` | 按钮、链接、高亮 |
| 次色 | `#764ba2` | 渐变、强调 |
| 深色 | `#1a1a2e` | 标题、深色背景 |
| 灰色 | `#888888` | 次要文字 |
| 边框 | `#e8e8e8` | 分割线、边框 |
| 背景 | `#f5f7fa` | 页面背景 |

### 8.3 CSS类命名规范

- 使用UnoCSS原子类优先
- 自定义类使用kebab-case命名
- 页面容器：`.page-container`
- 组件容器：`.component-name`

---

## 9. TypeScript类型

### 9.1 通用类型

```typescript
interface ApiResponse<T = any> {
  code: number
  message: string
  data: T
}

interface PaginationParams {
  page?: number
  size?: number
}

interface PaginatedResponse<T> {
  content: T[]
  totalElements: number
  totalPages: number
}
```

### 9.2 用户类型

```typescript
interface User {
  id: number
  username: string
  name: string
  email: string
  age: number
  role: 'USER' | 'COMPANY' | 'ADMIN'
  status: 'ACTIVE' | 'DISABLED'
  avatarUrl?: string
}

interface LoginRequest {
  username: string
  password: string
}

interface RegisterRequest {
  username: string
  password: string
  name: string
  email: string
  age: number
}
```

### 9.3 职位类型

```typescript
interface Job {
  id: number
  companyId: number
  companyName: string
  title: string
  description: string
  requirements: string
  salaryMin: number
  salaryMax: number
  location: string
  jobType: string
  education: string
  experience: string
  status: 'ACTIVE' | 'CLOSED' | 'DRAFT'
}

interface JobQueryParams {
  keyword?: string
  city?: string
  experience?: string
  education?: string
  salary?: string
}
```

---

## 10. 构建与部署

### 10.1 开发环境

```bash
cd frontend
npm install
npm run dev
```

访问：http://localhost:5173

### 10.2 生产构建

```bash
npm run build
```

输出目录：`dist/`

### 10.3 环境变量

```env
VITE_API_BASE_URL=/api
```

### 10.4 Nginx配置

```nginx
server {
    listen 80;
    server_name example.com;
    
    location / {
        root /var/www/frontend/dist;
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /uploads {
        proxy_pass http://localhost:8080;
    }
}
```

---

## 11. 开发规范

### 11.1 组件开发

- 使用 `<script setup>` 语法
- 使用 TypeScript 类型注解
- Props 使用 `defineProps` 定义
- Emits 使用 `defineEmits` 定义

### 11.2 API调用

- 统一使用 `api/` 目录下的封装函数
- 错误处理使用 try-catch
- Loading状态管理

### 11.3 代码风格

- 使用 ESLint + Prettier
- 缩进：2空格
- 引号：单引号
- 分号：不使用

---

## 12. 注意事项

1. **Token管理**：登录成功后存储到localStorage，每次请求自动携带
2. **路由守卫**：未登录访问需登录页面时跳转到登录页
3. **错误处理**：401错误自动跳转登录页
4. **文件上传**：使用FormData，支持头像、企业Logo等
5. **响应式设计**：适配桌面和移动端
