server:
  port: 8080

spring:
  application:
    name: agri-store-backend

  datasource:
    url: jdbc:mysql://47.97.108.222:3306/agri_store?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: kimi
    password: tR!9dB2x@7QwLpZ$mF8s
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false

jwt:
  secret: agristore-secret-key-2024-spring-boot-jwt
  expiration: 86400000  # 24 hours in milliseconds

cors:
  allowed-origins: "*"
