server:
  port: 8080

spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://47.97.108.222:3306/gp11?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: kimi
    password: tR!9dB2x@7QwLpZ$mF8s
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        use_sql_comments: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
      file-size-threshold: 2KB

jwt:
  secret: mySecretKeyForJwtTokenGeneration2024MySecretKey
  expiration: 86400000
  refresh-expiration: 604800000

file:
  upload:
    path: upload

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.example.backend: DEBUG
  file:
    name: logs/application.log
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
