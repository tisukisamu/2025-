server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://47.97.108.222:3306/gp9?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: kimi
    password: tR!9dB2x@7QwLpZ$mF8s
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
      
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false

jwt:
  secret: clubFundJwtSecretKey2024ClubFundPlatformSecretKeyForJwtToken
  expiration: 604800000

upload:
  path: ./upload
  max-size: 10485760
  allowed-types: jpg,jpeg,png,gif,pdf

logging:
  level:
    com.club.fund: debug
    org.hibernate.SQL: debug
