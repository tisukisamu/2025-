server:
  port: 8080

spring:
  application:
    name: backend

  # 数据库配置
  datasource:
    url: jdbc:mysql://47.97.108.222:3306/gp4?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: kimi
    password: tR!9dB2x@7QwLpZ$mF8s
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA / Hibernate 配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  # 文件上传配置
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 50MB

# JWT 配置
jwt:
  secret: mySecretKeyForJwtTokenGeneration2024MySecretKey
  expiration: 86400000  # 24小时

# 文件上传路径
file:
  upload-path: ./uploads

# 日志配置
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.example.backend: DEBUG
