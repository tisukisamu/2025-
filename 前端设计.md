# 校园闲置电子产品置换系统 - 前端设计文档

## 1. 技术架构

### 1.1 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue.js | 3.4.x | 前端框架 |
| Ant Design Vue | 4.1.x | UI组件库 |
| UnoCSS | 66.x | 原子化CSS框架 |
| Vite | 5.2.x | 构建工具 |
| Pinia | 2.1.x | 状态管理 |
| Vue Router | 4.3.x | 路由管理 |
| Axios | 1.6.x | HTTP请求库 |
| @ant-design/icons-vue | 7.0.x | 图标库 |

### 1.2 架构设计

```
┌─────────────────────────────────────────────────────────────────┐
│                          Vue 3 应用                              │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      视图层 (Views)                       │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │   │
│  │  │  首页   │ │ 商品页  │ │ 订单页  │ │ 个人中心│       │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    组件层 (Components)                    │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │   │
│  │  │ 公共组件│ │ 业务组件│ │ 布局组件│ │ 表单组件│       │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    状态层 (Stores - Pinia)                │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │   │
│  │  │  user   │ │ product │ │  order  │ │ message │       │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    服务层 (API Services)                  │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │   │
│  │  │ auth.js │ │ user.js │ │product.js│ │ order.js│       │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    工具层 (Utils)                         │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐                    │   │
│  │  │ request │ │ storage │ │ format  │                    │   │
│  │  └─────────┘ └─────────┘ └─────────┘                    │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## 2. 目录结构

```
frontend/
├── public/                     # 静态资源目录
│   └── vite.svg               # 网站图标
├── src/
│   ├── api/                   # API接口封装
│   │   ├── auth.js           # 认证相关API
│   │   ├── user.js           # 用户相关API
│   │   ├── product.js        # 商品相关API
│   │   ├── order.js          # 订单相关API
│   │   ├── review.js         # 评价相关API
│   │   ├── message.js        # 消息相关API
│   │   └── admin.js          # 管理后台API
│   ├── assets/                # 资源文件
│   │   ├── images/           # 图片资源
│   │   └── styles/           # 全局样式
│   ├── components/            # 公共组件
│   │   ├── common/           # 通用组件
│   │   │   ├── Pagination.vue
│   │   │   ├── Empty.vue
│   │   │   └── Loading.vue
│   │   ├── layout/           # 布局组件
│   │   │   ├── MainLayout.vue
│   │   │   ├── Header.vue
│   │   │   ├── Footer.vue
│   │   │   └── Sidebar.vue
│   │   ├── product/          # 商品相关组件
│   │   │   ├── ProductCard.vue
│   │   │   ├── ProductList.vue
│   │   │   ├── ProductFilter.vue
│   │   │   └── ImageUpload.vue
│   │   ├── order/            # 订单相关组件
│   │   │   ├── OrderCard.vue
│   │   │   ├── OrderStatus.vue
│   │   │   └── OrderTimeline.vue
│   │   └── chat/             # 聊天相关组件
│   │       ├── ChatWindow.vue
│   │       ├── MessageItem.vue
│   │       └── MessageInput.vue
│   ├── directives/            # 自定义指令
│   │   ├── permission.js     # 权限指令
│   │   └── lazy.js           # 懒加载指令
│   ├── hooks/                 # 组合式函数
│   │   ├── useAuth.js        # 认证相关
│   │   ├── usePagination.js  # 分页相关
│   │   └── useUpload.js      # 上传相关
│   ├── router/                # 路由配置
│   │   ├── index.js          # 路由入口
│   │   └── routes.js         # 路由定义
│   ├── stores/                # Pinia状态管理
│   │   ├── user.js           # 用户状态
│   │   ├── product.js        # 商品状态
│   │   ├── order.js          # 订单状态
│   │   └── message.js        # 消息状态
│   ├── utils/                 # 工具函数
│   │   ├── request.js        # Axios封装
│   │   ├── storage.js        # 本地存储
│   │   ├── format.js         # 格式化工具
│   │   └── validate.js       # 表单验证
│   ├── views/                 # 页面组件
│   │   ├── auth/             # 认证页面
│   │   │   ├── Login.vue
│   │   │   └── Register.vue
│   │   ├── home/             # 首页
│   │   │   └── Home.vue
│   │   ├── product/          # 商品页面
│   │   │   ├── ProductList.vue
│   │   │   ├── ProductDetail.vue
│   │   │   ├── ProductPublish.vue
│   │   │   └── MyProducts.vue
│   │   ├── order/            # 订单页面
│   │   │   ├── OrderList.vue
│   │   │   ├── OrderDetail.vue
│   │   │   └── CreateOrder.vue
│   │   ├── user/             # 用户页面
│   │   │   ├── Profile.vue
│   │   │   ├── Favorites.vue
│   │   │   └── Reviews.vue
│   │   ├── message/          # 消息页面
│   │   │   ├── MessageList.vue
│   │   │   └── Chat.vue
│   │   ├── admin/            # 管理后台
│   │   │   ├── Dashboard.vue
│   │   │   ├── UserManage.vue
│   │   │   ├── ProductManage.vue
│   │   │   ├── OrderManage.vue
│   │   │   └── Statistics.vue
│   │   └── error/            # 错误页面
│   │       └── NotFound.vue
│   ├── App.vue               # 根组件
│   └── main.js               # 入口文件
├── index.html                 # HTML模板
├── package.json              # 依赖配置
├── vite.config.js            # Vite配置
└── uno.config.js             # UnoCSS配置
```

## 3. 路由设计

### 3.1 路由配置

```javascript
const routes = [
  {
    path: '/',
    component: MainLayout,
    children: [
      {
        path: '',
        name: 'Home',
        component: () => import('@/views/home/Home.vue'),
        meta: { title: '首页' }
      },
      {
        path: 'products',
        name: 'ProductList',
        component: () => import('@/views/product/ProductList.vue'),
        meta: { title: '商品列表' }
      },
      {
        path: 'products/:id',
        name: 'ProductDetail',
        component: () => import('@/views/product/ProductDetail.vue'),
        meta: { title: '商品详情' }
      },
      {
        path: 'publish',
        name: 'ProductPublish',
        component: () => import('@/views/product/ProductPublish.vue'),
        meta: { title: '发布商品', requiresAuth: true }
      },
      {
        path: 'orders',
        name: 'OrderList',
        component: () => import('@/views/order/OrderList.vue'),
        meta: { title: '我的订单', requiresAuth: true }
      },
      {
        path: 'orders/:id',
        name: 'OrderDetail',
        component: () => import('@/views/order/OrderDetail.vue'),
        meta: { title: '订单详情', requiresAuth: true }
      },
      {
        path: 'profile',
        name: 'Profile',
        component: () => import('@/views/user/Profile.vue'),
        meta: { title: '个人中心', requiresAuth: true }
      },
      {
        path: 'favorites',
        name: 'Favorites',
        component: () => import('@/views/user/Favorites.vue'),
        meta: { title: '我的收藏', requiresAuth: true }
      },
      {
        path: 'messages',
        name: 'Messages',
        component: () => import('@/views/message/MessageList.vue'),
        meta: { title: '消息中心', requiresAuth: true }
      },
      {
        path: 'chat/:userId',
        name: 'Chat',
        component: () => import('@/views/message/Chat.vue'),
        meta: { title: '聊天', requiresAuth: true }
      },
      {
        path: 'admin',
        name: 'Admin',
        component: () => import('@/views/admin/Dashboard.vue'),
        meta: { title: '管理后台', requiresAuth: true, requiresAdmin: true },
        children: [
          {
            path: 'users',
            name: 'AdminUsers',
            component: () => import('@/views/admin/UserManage.vue')
          },
          {
            path: 'products',
            name: 'AdminProducts',
            component: () => import('@/views/admin/ProductManage.vue')
          },
          {
            path: 'orders',
            name: 'AdminOrders',
            component: () => import('@/views/admin/OrderManage.vue')
          },
          {
            path: 'statistics',
            name: 'AdminStatistics',
            component: () => import('@/views/admin/Statistics.vue')
          }
        ]
      }
    ]
  },
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/auth/Login.vue'),
    meta: { title: '登录', guest: true }
  },
  {
    path: '/register',
    name: 'Register',
    component: () => import('@/views/auth/Register.vue'),
    meta: { title: '注册', guest: true }
  },
  {
    path: '/:pathMatch(.*)*',
    name: 'NotFound',
    component: () => import('@/views/error/NotFound.vue'),
    meta: { title: '页面不存在' }
  }
]
```

### 3.2 路由守卫

```javascript
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  const isLoggedIn = userStore.isLoggedIn
  const isAdmin = userStore.isAdmin

  document.title = to.meta.title ? `${to.meta.title} - 校园置换` : '校园闲置电子产品置换系统'

  if (to.meta.requiresAuth && !isLoggedIn) {
    next({ name: 'Login', query: { redirect: to.fullPath } })
  } else if (to.meta.requiresAdmin && !isAdmin) {
    next({ name: 'Home' })
  } else if (to.meta.guest && isLoggedIn) {
    next({ name: 'Home' })
  } else {
    next()
  }
})
```

## 4. 状态管理设计

### 4.1 用户状态 (user.js)

```javascript
import { defineStore } from 'pinia'
import { login, register, logout, getUserInfo } from '@/api/auth'

export const useUserStore = defineStore('user', {
  state: () => ({
    token: localStorage.getItem('token') || null,
    userInfo: null,
    isLoggedIn: false
  }),

  getters: {
    isAdmin: (state) => state.userInfo?.role === 'ADMIN',
    username: (state) => state.userInfo?.username || '',
    avatar: (state) => state.userInfo?.avatar || ''
  },

  actions: {
    async login(credentials) {
      const res = await login(credentials)
      this.token = res.data.token
      this.userInfo = res.data.user
      this.isLoggedIn = true
      localStorage.setItem('token', res.data.token)
    },

    async register(userData) {
      await register(userData)
    },

    async logout() {
      await logout()
      this.token = null
      this.userInfo = null
      this.isLoggedIn = false
      localStorage.removeItem('token')
    },

    async fetchUserInfo() {
      if (this.token) {
        const res = await getUserInfo()
        this.userInfo = res.data
        this.isLoggedIn = true
      }
    }
  }
})
```

### 4.2 商品状态 (product.js)

```javascript
import { defineStore } from 'pinia'
import { getProducts, getProductDetail, getCategories } from '@/api/product'

export const useProductStore = defineStore('product', {
  state: () => ({
    products: [],
    currentProduct: null,
    categories: [],
    filters: {
      keyword: '',
      categoryId: null,
      minPrice: null,
      maxPrice: null,
      sortBy: 'createTime',
      sortOrder: 'desc'
    },
    pagination: {
      page: 1,
      pageSize: 12,
      total: 0
    }
  }),

  actions: {
    async fetchProducts() {
      const res = await getProducts({
        ...this.filters,
        page: this.pagination.page,
        pageSize: this.pagination.pageSize
      })
      this.products = res.data.list
      this.pagination.total = res.data.total
    },

    async fetchProductDetail(id) {
      const res = await getProductDetail(id)
      this.currentProduct = res.data
    },

    async fetchCategories() {
      const res = await getCategories()
      this.categories = res.data
    },

    setFilters(filters) {
      this.filters = { ...this.filters, ...filters }
      this.pagination.page = 1
    },

    setPage(page) {
      this.pagination.page = page
    }
  }
})
```

### 4.3 消息状态 (message.js)

```javascript
import { defineStore } from 'pinia'
import { getConversations, getMessages, sendMessage } from '@/api/message'

export const useMessageStore = defineStore('message', {
  state: () => ({
    conversations: [],
    currentConversation: null,
    messages: [],
    unreadCount: 0,
    websocket: null
  }),

  actions: {
    async fetchConversations() {
      const res = await getConversations()
      this.conversations = res.data
      this.unreadCount = res.data.reduce((sum, c) => sum + c.unreadCount, 0)
    },

    async fetchMessages(userId) {
      const res = await getMessages(userId)
      this.messages = res.data
    },

    async send(data) {
      await sendMessage(data)
      this.messages.push(data)
    },

    connectWebSocket() {
      // WebSocket连接逻辑
    }
  }
})
```

## 5. API 接口封装

### 5.1 Axios 请求封装 (utils/request.js)

```javascript
import axios from 'axios'
import { message } from 'ant-design-vue'
import router from '@/router'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api',
  timeout: 10000
})

request.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

request.interceptors.response.use(
  (response) => {
    const res = response.data
    if (res.code !== 200) {
      message.error(res.message || '请求失败')
      return Promise.reject(new Error(res.message || '请求失败'))
    }
    return res
  },
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      router.push('/login')
      message.error('登录已过期，请重新登录')
    } else if (error.response?.status === 403) {
      message.error('没有权限访问')
    } else {
      message.error(error.message || '网络错误')
    }
    return Promise.reject(error)
  }
)

export default request
```

### 5.2 API 模块定义

#### 认证模块 (api/auth.js)
```javascript
import request from '@/utils/request'

export function login(data) {
  return request.post('/auth/login', data)
}

export function register(data) {
  return request.post('/auth/register', data)
}

export function logout() {
  return request.post('/auth/logout')
}

export function refreshToken() {
  return request.post('/auth/refresh')
}
```

#### 用户模块 (api/user.js)
```javascript
import request from '@/utils/request'

export function getUserInfo() {
  return request.get('/users/me')
}

export function updateUserInfo(data) {
  return request.put('/users/me', data)
}

export function updatePassword(data) {
  return request.put('/users/me/password', data)
}

export function getUserProducts(params) {
  return request.get('/users/me/products', { params })
}

export function getUserOrders(params) {
  return request.get('/users/me/orders', { params })
}

export function getUserFavorites(params) {
  return request.get('/users/me/favorites', { params })
}
```

#### 商品模块 (api/product.js)
```javascript
import request from '@/utils/request'

export function getProducts(params) {
  return request.get('/products', { params })
}

export function getProductDetail(id) {
  return request.get(`/products/${id}`)
}

export function createProduct(data) {
  return request.post('/products', data)
}

export function updateProduct(id, data) {
  return request.put(`/products/${id}`, data)
}

export function deleteProduct(id) {
  return request.delete(`/products/${id}`)
}

export function getCategories() {
  return request.get('/categories')
}

export function uploadImage(file) {
  const formData = new FormData()
  formData.append('file', file)
  return request.post('/upload', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  })
}
```

#### 订单模块 (api/order.js)
```javascript
import request from '@/utils/request'

export function getOrders(params) {
  return request.get('/orders', { params })
}

export function getOrderDetail(id) {
  return request.get(`/orders/${id}`)
}

export function createOrder(data) {
  return request.post('/orders', data)
}

export function shipOrder(id, data) {
  return request.put(`/orders/${id}/ship`, data)
}

export function confirmOrder(id) {
  return request.put(`/orders/${id}/confirm`)
}

export function cancelOrder(id, reason) {
  return request.put(`/orders/${id}/cancel`, { reason })
}
```

## 6. 页面设计

### 6.1 页面列表

| 页面名称 | 路由 | 功能描述 |
|----------|------|----------|
| 首页 | / | 商品展示、分类导航、搜索入口 |
| 登录 | /login | 用户登录 |
| 注册 | /register | 用户注册 |
| 商品列表 | /products | 商品搜索、筛选、排序 |
| 商品详情 | /products/:id | 商品详细信息、卖家信息、立即购买 |
| 发布商品 | /publish | 商品发布表单、图片上传 |
| 我的商品 | /products/mine | 我发布的商品管理 |
| 订单列表 | /orders | 我的订单列表（买家/卖家视角） |
| 订单详情 | /orders/:id | 订单详细信息、状态流转 |
| 个人中心 | /profile | 个人信息管理 |
| 我的收藏 | /favorites | 收藏商品列表 |
| 消息中心 | /messages | 消息列表、系统通知 |
| 聊天 | /chat/:userId | 与对方即时通讯 |
| 管理后台 | /admin | 管理员后台首页 |
| 用户管理 | /admin/users | 用户管理 |
| 商品管理 | /admin/products | 商品审核、管理 |
| 订单管理 | /admin/orders | 订单管理 |
| 数据统计 | /admin/statistics | 数据看板 |

### 6.2 页面布局

#### 主布局 (MainLayout)
```
┌─────────────────────────────────────────────────────────────┐
│                        Header                                │
│  Logo    搜索框      首页  发布  消息  个人中心   登录/头像   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                                                             │
│                      Main Content                           │
│                      (router-view)                          │
│                                                             │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                        Footer                               │
│              关于我们 | 联系方式 | 使用条款                  │
└─────────────────────────────────────────────────────────────┘
```

#### 管理后台布局
```
┌─────────────────────────────────────────────────────────────┐
│                        Header                                │
│  Logo                              管理员信息   退出登录     │
├──────────────┬──────────────────────────────────────────────┤
│              │                                              │
│   Sidebar    │              Main Content                    │
│              │              (router-view)                   │
│  仪表盘      │                                              │
│  用户管理    │                                              │
│  商品管理    │                                              │
│  订单管理    │                                              │
│  数据统计    │                                              │
│              │                                              │
└──────────────┴──────────────────────────────────────────────┘
```

## 7. 组件设计

### 7.1 商品卡片组件 (ProductCard.vue)

```vue
<template>
  <a-card hoverable class="product-card" @click="goToDetail">
    <template #cover>
      <img :src="product.coverImage" :alt="product.title" />
    </template>
    <a-card-meta>
      <template #title>
        <div class="product-title">{{ product.title }}</div>
      </template>
      <template #description>
        <div class="product-price">¥{{ product.price }}</div>
        <div class="product-info">
          <span>{{ product.categoryName }}</span>
          <span>{{ formatTime(product.createTime) }}</span>
        </div>
      </template>
    </a-card-meta>
  </a-card>
</template>

<script setup>
import { useRouter } from 'vue-router'
import { formatTime } from '@/utils/format'

const props = defineProps({
  product: {
    type: Object,
    required: true
  }
})

const router = useRouter()

const goToDetail = () => {
  router.push(`/products/${props.product.id}`)
}
</script>
```

### 7.2 订单状态组件 (OrderStatus.vue)

```vue
<template>
  <a-steps :current="currentStep" :status="stepStatus">
    <a-step title="待发货" :description="order.createTime" />
    <a-step title="待收货" :description="order.shipTime" />
    <a-step title="已完成" :description="order.completeTime" />
  </a-steps>
</template>

<script setup>
import { computed } from 'vue'

const props = defineProps({
  order: {
    type: Object,
    required: true
  }
})

const statusMap = {
  PENDING: 0,
  SHIPPED: 1,
  COMPLETED: 2,
  CANCELLED: -1
}

const currentStep = computed(() => statusMap[props.order.status] || 0)

const stepStatus = computed(() => {
  return props.order.status === 'CANCELLED' ? 'error' : 'process'
})
</script>
```

### 7.3 聊天窗口组件 (ChatWindow.vue)

```vue
<template>
  <div class="chat-window">
    <div class="chat-header">
      <a-avatar :src="targetUser.avatar" />
      <span class="username">{{ targetUser.username }}</span>
    </div>
    
    <div class="chat-messages" ref="messageContainer">
      <message-item
        v-for="msg in messages"
        :key="msg.id"
        :message="msg"
        :is-self="msg.senderId === currentUserId"
      />
    </div>
    
    <div class="chat-input">
      <a-input
        v-model:value="inputMessage"
        placeholder="输入消息..."
        @pressEnter="send"
      />
      <a-button type="primary" @click="send">发送</a-button>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted, nextTick } from 'vue'
import { useUserStore } from '@/stores/user'
import { useMessageStore } from '@/stores/message'

const props = defineProps({
  targetUser: Object,
  messages: Array
})

const userStore = useUserStore()
const messageStore = useMessageStore()

const inputMessage = ref('')
const messageContainer = ref(null)
const currentUserId = userStore.userInfo?.id

const send = async () => {
  if (!inputMessage.value.trim()) return
  
  await messageStore.send({
    receiverId: props.targetUser.id,
    content: inputMessage.value,
    type: 'TEXT'
  })
  
  inputMessage.value = ''
  scrollToBottom()
}

const scrollToBottom = () => {
  nextTick(() => {
    messageContainer.value.scrollTop = messageContainer.value.scrollHeight
  })
}

onMounted(() => {
  scrollToBottom()
})
</script>
```

## 8. 样式设计

### 8.1 UnoCSS 配置 (uno.config.js)

```javascript
import { defineConfig, presetUno, presetAttributify, presetIcons } from 'unocss'

export default defineConfig({
  presets: [
    presetUno(),
    presetAttributify(),
    presetIcons()
  ],
  theme: {
    colors: {
      primary: '#1890ff',
      success: '#52c41a',
      warning: '#faad14',
      error: '#f5222d',
      info: '#1890ff'
    }
  },
  shortcuts: {
    'flex-center': 'flex items-center justify-center',
    'flex-between': 'flex items-center justify-between',
    'text-ellipsis': 'overflow-hidden whitespace-nowrap text-overflow-ellipsis'
  }
})
```

### 8.2 全局样式

```css
:root {
  --primary-color: #1890ff;
  --success-color: #52c41a;
  --warning-color: #faad14;
  --error-color: #f5222d;
  --text-color: rgba(0, 0, 0, 0.85);
  --text-secondary: rgba(0, 0, 0, 0.45);
  --border-color: #d9d9d9;
  --bg-color: #f5f5f5;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  color: var(--text-color);
  background-color: var(--bg-color);
}

.page-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 24px;
}

.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
  gap: 16px;
}
```

## 9. 构建配置

### 9.1 Vite 配置 (vite.config.js)

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import UnoCSS from 'unocss/vite'
import { resolve } from 'path'

export default defineConfig({
  plugins: [
    vue(),
    UnoCSS()
  ],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  },
  build: {
    outDir: 'dist',
    sourcemap: false,
    rollupOptions: {
      output: {
        chunkFileNames: 'assets/js/[name]-[hash].js',
        entryFileNames: 'assets/js/[name]-[hash].js',
        assetFileNames: 'assets/[ext]/[name]-[hash].[ext]'
      }
    }
  }
})
```

### 9.2 环境变量

```
# .env.development
VITE_API_BASE_URL=/api

# .env.production
VITE_API_BASE_URL=https://api.domain.com
```

## 10. 性能优化

### 10.1 路由懒加载
```javascript
component: () => import('@/views/product/ProductList.vue')
```

### 10.2 图片懒加载
```vue
<img v-lazy="imageUrl" />
```

### 10.3 组件按需引入
```javascript
import { Button, Card, Input } from 'ant-design-vue'
```

### 10.4 虚拟列表
对于长列表数据使用虚拟滚动优化性能。

### 10.5 缓存策略
- 使用 Pinia 持久化用户状态
- 合理使用 keep-alive 缓存页面
- API 响应数据缓存

## 11. 开发规范

### 11.1 命名规范
- 组件文件：PascalCase (如 ProductCard.vue)
- 普通文件：camelCase (如 request.js)
- CSS类名：kebab-case (如 product-card)

### 11.2 组件规范
- 使用 Composition API
- 使用 `<script setup>` 语法
- Props 定义使用 TypeScript 或明确类型

### 11.3 Git 提交规范
```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式
refactor: 重构
test: 测试
chore: 构建/工具
```
