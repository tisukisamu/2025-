# 街舞工作室管理系统 - 后端架构文档

## 1. 技术栈

- **框架**: Spring Boot 2.7+
- **ORM**: Hibernate / Spring Data JPA
- **数据库**: MySQL 8.0+
- **认证授权**: Spring Security + JWT
- **构建工具**: Maven
- **开发语言**: Java 11+

## 2. 项目结构

```
backend/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/
│   │   │       └── example/
│   │   │           └── backend/
│   │   │               ├── config/           # 配置类
│   │   │               │   ├── SecurityConfig.java
│   │   │               │   ├── WebConfig.java
│   │   │               │   ├── GlobalExceptionHandler.java
│   │   │               │   ├── SwaggerConfig.java
│   │   │               │   └── CorsConfig.java
│   │   │               ├── controller/       # 控制器层
│   │   │               │   ├── AuthController.java
│   │   │               │   ├── UserController.java
│   │   │               │   ├── AdminController.java
│   │   │               │   ├── CourseController.java
│   │   │               │   ├── TeacherController.java
│   │   │               │   ├── StudentController.java
│   │   │               │   ├── FinanceController.java
│   │   │               │   └── StatisticsController.java
│   │   │               ├── service/          # 服务层
│   │   │               │   ├── AuthService.java
│   │   │               │   ├── UserService.java
│   │   │               │   ├── AdminService.java
│   │   │               │   ├── CourseService.java
│   │   │               │   ├── TeacherService.java
│   │   │               │   ├── StudentService.java
│   │   │               │   ├── FinanceService.java
│   │   │               │   └── StatisticsService.java
│   │   │               ├── repository/       # 数据访问层
│   │   │               │   ├── UserRepository.java
│   │   │               │   ├── CourseRepository.java
│   │   │               │   ├── TeacherRepository.java
│   │   │               │   ├── StudentRepository.java
│   │   │               │   ├── EnrollmentRepository.java
│   │   │               │   ├── ScheduleRepository.java
│   │   │               │   ├── PaymentRepository.java
│   │   │               │   └── NotificationRepository.java
│   │   │               ├── entity/           # 实体类
│   │   │               │   ├── User.java
│   │   │               │   ├── Course.java
│   │   │               │   ├── Teacher.java
│   │   │               │   ├── Student.java
│   │   │               │   ├── Enrollment.java
│   │   │               │   ├── Schedule.java
│   │   │               │   ├── Payment.java
│   │   │               │   └── Notification.java
│   │   │               ├── dto/              # 数据传输对象
│   │   │               │   ├── request/      # 请求DTO
│   │   │               │   │   ├── LoginRequest.java
│   │   │               │   │   ├── RegisterRequest.java
│   │   │               │   │   ├── CourseRequest.java
│   │   │               │   │   ├── EnrollmentRequest.java
│   │   │               │   │   └── PaymentRequest.java
│   │   │               │   └── response/     # 响应DTO
│   │   │               │       ├── AuthResponse.java
│   │   │               │       ├── UserDTO.java
│   │   │               │       ├── CourseDTO.java
│   │   │               │       └── StatisticsDTO.java
│   │   │               ├── security/         # 安全相关
│   │   │               │   ├── JwtUtil.java
│   │   │               │   ├── JwtAuthenticationFilter.java
│   │   │               │   ├── UserDetailsImpl.java
│   │   │               │   └── UserDetailsServiceImpl.java
│   │   │               ├── exception/        # 异常处理
│   │   │               │   ├── BusinessException.java
│   │   │               │   ├── ResourceNotFoundException.java
│   │   │               │   └── ValidationException.java
│   │   │               ├── util/             # 工具类
│   │   │               │   ├── FileUploadUtil.java
│   │   │               │   ├── DateUtil.java
│   │   │               │   └── ValidationUtil.java
│   │   │               └── BackendApplication.java
│   │   └── resources/
│   │       ├── application.yml               # 应用配置
│   │       ├── application-dev.yml           # 开发环境配置
│   │       ├── application-prod.yml          # 生产环境配置
│   │       └── db/                           # 数据库脚本
│   │           ├── schema.sql                # 表结构
│   │           └── data.sql                  # 初始数据
│   └── test/                                 # 测试代码
│       └── java/
│           └── com/
│               └── example/
│                   └── backend/
│                       ├── controller/
│                       ├── service/
│                       └── repository/
├── upload/                                    # 文件上传目录
│   ├── avatars/                              # 用户头像
│   ├── course-images/                        # 课程图片
│   └── documents/                            # 文档文件
├── pom.xml
└── README.md
```

## 3. 数据库设计

### 3.1 用户表 (users)
```sql
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    email VARCHAR(100) UNIQUE,
    phone VARCHAR(20) UNIQUE,
    real_name VARCHAR(50),
    avatar VARCHAR(255),
    role ENUM('ADMIN', 'TEACHER', 'STUDENT') NOT NULL,
    status ENUM('ACTIVE', 'INACTIVE', 'BANNED') DEFAULT 'ACTIVE',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_username (username),
    INDEX idx_email (email),
    INDEX idx_phone (phone)
);
```

### 3.2 教师表 (teachers)
```sql
CREATE TABLE teachers (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT UNIQUE,
    name VARCHAR(50) NOT NULL,
    phone VARCHAR(20),
    email VARCHAR(100),
    subjects VARCHAR(255),
    experience INT,
    bio TEXT,
    avatar VARCHAR(255),
    status ENUM('ACTIVE', 'INACTIVE') DEFAULT 'ACTIVE',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL,
    INDEX idx_name (name),
    INDEX idx_status (status)
);
```

### 3.3 学员表 (students)
```sql
CREATE TABLE students (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT UNIQUE,
    name VARCHAR(50) NOT NULL,
    phone VARCHAR(20),
    email VARCHAR(100),
    gender ENUM('MALE', 'FEMALE', 'OTHER'),
    birth_date DATE,
    emergency_contact VARCHAR(50),
    emergency_phone VARCHAR(20),
    avatar VARCHAR(255),
    status ENUM('ACTIVE', 'INACTIVE') DEFAULT 'ACTIVE',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL,
    INDEX idx_name (name),
    INDEX idx_status (status)
);
```

### 3.4 课程表 (courses)
```sql
CREATE TABLE courses (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    teacher_id BIGINT,
    category VARCHAR(50),
    level ENUM('BEGINNER', 'INTERMEDIATE', 'ADVANCED'),
    duration INT,
    price DECIMAL(10, 2) NOT NULL,
    capacity INT DEFAULT 20,
    enrolled_count INT DEFAULT 0,
    image VARCHAR(255),
    status ENUM('DRAFT', 'PUBLISHED', 'CLOSED') DEFAULT 'DRAFT',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (teacher_id) REFERENCES teachers(id) ON DELETE SET NULL,
    INDEX idx_name (name),
    INDEX idx_teacher_id (teacher_id),
    INDEX idx_status (status)
);
```

### 3.5 课程排班表 (schedules)
```sql
CREATE TABLE schedules (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    course_id BIGINT NOT NULL,
    teacher_id BIGINT NOT NULL,
    start_time DATETIME NOT NULL,
    end_time DATETIME NOT NULL,
    location VARCHAR(100),
    room VARCHAR(50),
    is_recurring BOOLEAN DEFAULT FALSE,
    recurrence_pattern VARCHAR(50),
    status ENUM('SCHEDULED', 'CANCELLED', 'COMPLETED') DEFAULT 'SCHEDULED',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (course_id) REFERENCES courses(id) ON DELETE CASCADE,
    FOREIGN KEY (teacher_id) REFERENCES teachers(id) ON DELETE CASCADE,
    INDEX idx_course_id (course_id),
    INDEX idx_teacher_id (teacher_id),
    INDEX idx_start_time (start_time)
);
```

### 3.6 报名表 (enrollments)
```sql
CREATE TABLE enrollments (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    student_id BIGINT NOT NULL,
    course_id BIGINT NOT NULL,
    schedule_id BIGINT,
    enrollment_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    status ENUM('PENDING', 'CONFIRMED', 'CANCELLED', 'COMPLETED') DEFAULT 'PENDING',
    payment_status ENUM('UNPAID', 'PAID', 'REFUNDED') DEFAULT 'UNPAID',
    cancel_reason VARCHAR(255),
    cancelled_at TIMESTAMP NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (student_id) REFERENCES students(id) ON DELETE CASCADE,
    FOREIGN KEY (course_id) REFERENCES courses(id) ON DELETE CASCADE,
    FOREIGN KEY (schedule_id) REFERENCES schedules(id) ON DELETE SET NULL,
    INDEX idx_student_id (student_id),
    INDEX idx_course_id (course_id),
    INDEX idx_status (status)
);
```

### 3.7 支付表 (payments)
```sql
CREATE TABLE payments (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    enrollment_id BIGINT NOT NULL,
    amount DECIMAL(10, 2) NOT NULL,
    payment_method ENUM('ALIPAY', 'WECHAT', 'CASH', 'CARD'),
    transaction_id VARCHAR(100),
    status ENUM('PENDING', 'SUCCESS', 'FAILED', 'REFUNDED') DEFAULT 'PENDING',
    payment_time TIMESTAMP NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (enrollment_id) REFERENCES enrollments(id) ON DELETE CASCADE,
    INDEX idx_enrollment_id (enrollment_id),
    INDEX idx_status (status),
    INDEX idx_transaction_id (transaction_id)
);
```

### 3.8 账单表 (bills)
```sql
CREATE TABLE bills (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    student_id BIGINT NOT NULL,
    bill_type ENUM('TUITION', 'REGISTRATION', 'OTHER'),
    amount DECIMAL(10, 2) NOT NULL,
    description VARCHAR(255),
    due_date DATE,
    status ENUM('UNPAID', 'PAID', 'OVERDUE') DEFAULT 'UNPAID',
    payment_id BIGINT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (student_id) REFERENCES students(id) ON DELETE CASCADE,
    FOREIGN KEY (payment_id) REFERENCES payments(id) ON DELETE SET NULL,
    INDEX idx_student_id (student_id),
    INDEX idx_status (status)
);
```

### 3.9 通知表 (notifications)
```sql
CREATE TABLE notifications (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(100) NOT NULL,
    content TEXT,
    type ENUM('SYSTEM', 'COURSE', 'PAYMENT', 'GENERAL'),
    target_role ENUM('ALL', 'ADMIN', 'TEACHER', 'STUDENT'),
    is_published BOOLEAN DEFAULT FALSE,
    published_at TIMESTAMP NULL,
    created_by BIGINT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE SET NULL,
    INDEX idx_type (type),
    INDEX idx_is_published (is_published)
);
```

### 3.10 系统设置表 (settings)
```sql
CREATE TABLE settings (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    setting_key VARCHAR(100) UNIQUE NOT NULL,
    setting_value TEXT,
    description VARCHAR(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_setting_key (setting_key)
);
```

## 4. 核心功能模块

### 4.1 用户管理模块

#### AuthController
```java
@RestController
@RequestMapping("/api/auth")
public class AuthController {
    @PostMapping("/register")
    public ResponseEntity<AuthResponse> register(@RequestBody RegisterRequest request);
    
    @PostMapping("/login")
    public ResponseEntity<AuthResponse> login(@RequestBody LoginRequest request);
    
    @PostMapping("/logout")
    public ResponseEntity<Void> logout();
    
    @PostMapping("/refresh")
    public ResponseEntity<AuthResponse> refreshToken();
}
```

#### UserService
```java
@Service
public class UserService {
    User createUser(UserDTO userDTO);
    User updateUser(Long id, UserDTO userDTO);
    void deleteUser(Long id);
    User getUserById(Long id);
    Page<User> getUsers(Pageable pageable);
    void updateProfile(Long id, UserDTO userDTO);
    String uploadAvatar(Long id, MultipartFile file);
}
```

### 4.2 课程管理模块

#### CourseController
```java
@RestController
@RequestMapping("/api/courses")
public class CourseController {
    @GetMapping
    public ResponseEntity<Page<CourseDTO>> getCourses(Pageable pageable);
    
    @GetMapping("/{id}")
    public ResponseEntity<CourseDTO> getCourseById(@PathVariable Long id);
    
    @PostMapping
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<CourseDTO> createCourse(@RequestBody CourseRequest request);
    
    @PutMapping("/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<CourseDTO> updateCourse(@PathVariable Long id, @RequestBody CourseRequest request);
    
    @DeleteMapping("/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<Void> deleteCourse(@PathVariable Long id);
    
    @GetMapping("/{id}/schedules")
    public ResponseEntity<List<ScheduleDTO>> getCourseSchedules(@PathVariable Long id);
    
    @PostMapping("/{id}/enroll")
    @PreAuthorize("hasRole('STUDENT')")
    public ResponseEntity<EnrollmentDTO> enrollCourse(@PathVariable Long id, @RequestBody EnrollmentRequest request);
}
```

#### CourseService
```java
@Service
public class CourseService {
    Course createCourse(CourseRequest request);
    Course updateCourse(Long id, CourseRequest request);
    void deleteCourse(Long id);
    Course getCourseById(Long id);
    Page<Course> getCourses(Pageable pageable, CourseFilter filter);
    Schedule createSchedule(Long courseId, ScheduleRequest request);
    List<Schedule> getCourseSchedules(Long courseId);
    void checkCapacity(Long courseId);
    Enrollment enrollCourse(Long courseId, Long studentId, EnrollmentRequest request);
}
```

### 4.3 教师管理模块

#### TeacherController
```java
@RestController
@RequestMapping("/api/teachers")
public class TeacherController {
    @GetMapping
    public ResponseEntity<Page<TeacherDTO>> getTeachers(Pageable pageable);
    
    @GetMapping("/{id}")
    public ResponseEntity<TeacherDTO> getTeacherById(@PathVariable Long id);
    
    @PostMapping
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<TeacherDTO> createTeacher(@RequestBody TeacherRequest request);
    
    @PutMapping("/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<TeacherDTO> updateTeacher(@PathVariable Long id, @RequestBody TeacherRequest request);
    
    @DeleteMapping("/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<Void> deleteTeacher(@PathVariable Long id);
    
    @GetMapping("/{id}/courses")
    public ResponseEntity<List<CourseDTO>> getTeacherCourses(@PathVariable Long id);
    
    @GetMapping("/{id}/schedules")
    public ResponseEntity<List<ScheduleDTO>> getTeacherSchedules(@PathVariable Long id);
}
```

### 4.4 学员管理模块

#### StudentController
```java
@RestController
@RequestMapping("/api/students")
public class StudentController {
    @GetMapping
    @PreAuthorize("hasAnyRole('ADMIN', 'TEACHER')")
    public ResponseEntity<Page<StudentDTO>> getStudents(Pageable pageable);
    
    @GetMapping("/{id}")
    public ResponseEntity<StudentDTO> getStudentById(@PathVariable Long id);
    
    @PutMapping("/{id}")
    public ResponseEntity<StudentDTO> updateStudent(@PathVariable Long id, @RequestBody StudentRequest request);
    
    @GetMapping("/{id}/enrollments")
    public ResponseEntity<List<EnrollmentDTO>> getStudentEnrollments(@PathVariable Long id);
    
    @GetMapping("/{id}/courses")
    public ResponseEntity<List<CourseDTO>> getStudentCourses(@PathVariable Long id);
    
    @DeleteMapping("/enrollments/{enrollmentId}")
    public ResponseEntity<Void> cancelEnrollment(@PathVariable Long enrollmentId);
}
```

### 4.5 财务管理模块

#### FinanceController
```java
@RestController
@RequestMapping("/api/finance")
public class FinanceController {
    @PostMapping("/payment")
    @PreAuthorize("hasRole('STUDENT')")
    public ResponseEntity<PaymentDTO> createPayment(@RequestBody PaymentRequest request);
    
    @GetMapping("/payments")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<Page<PaymentDTO>> getPayments(Pageable pageable);
    
    @GetMapping("/payments/{id}")
    public ResponseEntity<PaymentDTO> getPaymentById(@PathVariable Long id);
    
    @PostMapping("/payments/{id}/refund")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<PaymentDTO> refundPayment(@PathVariable Long id);
    
    @GetMapping("/bills")
    public ResponseEntity<Page<BillDTO>> getBills(Pageable pageable);
    
    @PostMapping("/bills")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<BillDTO> createBill(@RequestBody BillRequest request);
}
```

#### FinanceService
```java
@Service
public class FinanceService {
    Payment createPayment(PaymentRequest request);
    Payment processPayment(Long paymentId, PaymentMethod method);
    Payment refundPayment(Long paymentId);
    Bill createBill(BillRequest request);
    Page<Payment> getPayments(Pageable pageable, PaymentFilter filter);
    Page<Bill> getBills(Pageable pageable, BillFilter filter);
    BigDecimal calculateTotalIncome(LocalDate start, LocalDate end);
    Map<String, BigDecimal> getIncomeByCategory(LocalDate start, LocalDate end);
}
```

### 4.6 统计报表模块

#### StatisticsController
```java
@RestController
@RequestMapping("/api/statistics")
public class StatisticsController {
    @GetMapping("/courses")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<CourseStatistics> getCourseStatistics();
    
    @GetMapping("/finance")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<FinanceStatistics> getFinanceStatistics(
        @RequestParam(required = false) LocalDate start,
        @RequestParam(required = false) LocalDate end);
    
    @GetMapping("/students")
    @PreAuthorize("hasAnyRole('ADMIN', 'TEACHER')")
    public ResponseEntity<StudentStatistics> getStudentStatistics();
    
    @GetMapping("/income/monthly")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<List<MonthlyIncome>> getMonthlyIncome(@RequestParam int year);
}
```

### 4.7 系统设置模块

#### SettingsController
```java
@RestController
@RequestMapping("/api/settings")
public class SettingsController {
    @GetMapping
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<List<SettingDTO>> getSettings();
    
    @PutMapping("/{key}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<SettingDTO> updateSetting(@PathVariable String key, @RequestBody SettingRequest request);
    
    @PostMapping("/notifications")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<NotificationDTO> createNotification(@RequestBody NotificationRequest request);
    
    @GetMapping("/notifications")
    public ResponseEntity<Page<NotificationDTO>> getNotifications(Pageable pageable);
}
```

## 5. 安全配置

### 5.1 Spring Security配置
```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .cors().and()
            .csrf().disable()
            .sessionManagement().sessionCreationPolicy(SessionCreationPolicy.STATELESS)
            .and()
            .authorizeRequests()
                .antMatchers("/api/auth/**").permitAll()
                .antMatchers("/api/admin/**").hasRole("ADMIN")
                .antMatchers("/api/teacher/**").hasRole("TEACHER")
                .antMatchers("/api/student/**").hasRole("STUDENT")
                .anyRequest().authenticated()
            .and()
            .addFilterBefore(jwtAuthenticationFilter(), UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }
}
```

### 5.2 JWT工具类
```java
@Component
public class JwtUtil {
    String generateToken(UserDetails userDetails);
    String generateRefreshToken(UserDetails userDetails);
    String extractUsername(String token);
    boolean validateToken(String token, UserDetails userDetails);
    boolean isTokenExpired(String token);
}
```

### 5.3 JWT过滤器
```java
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    @Override
    protected void doFilterInternal(HttpServletRequest request, 
                                    HttpServletResponse response, 
                                    FilterChain filterChain);
}
```

## 6. 异常处理

### 6.1 全局异常处理器
```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(ResourceNotFoundException.class)
    public ResponseEntity<ErrorResponse> handleResourceNotFound(ResourceNotFoundException ex);
    
    @ExceptionHandler(BusinessException.class)
    public ResponseEntity<ErrorResponse> handleBusinessException(BusinessException ex);
    
    @ExceptionHandler(ValidationException.class)
    public ResponseEntity<ErrorResponse> handleValidationException(ValidationException ex);
    
    @ExceptionHandler(AccessDeniedException.class)
    public ResponseEntity<ErrorResponse> handleAccessDenied(AccessDeniedException ex);
    
    @ExceptionHandler(Exception.class)
    public ResponseEntity<ErrorResponse> handleGenericException(Exception ex);
}
```

### 6.2 自定义异常
```java
public class BusinessException extends RuntimeException {
    private String errorCode;
    private String message;
}

public class ResourceNotFoundException extends RuntimeException {
    private String resourceName;
    private Long resourceId;
}

public class ValidationException extends RuntimeException {
    private List<String> errors;
}
```

## 7. 文件上传

### 7.1 文件上传工具类
```java
@Component
public class FileUploadUtil {
    String uploadFile(MultipartFile file, String uploadDir);
    void deleteFile(String filePath);
    boolean isValidImageFile(MultipartFile file);
    boolean isValidFileSize(MultipartFile file, long maxSize);
}
```

### 7.2 文件存储策略
- 存储路径: `upload/`
- 子目录:
  - `avatars/` - 用户头像
  - `course-images/` - 课程图片
  - `documents/` - 文档文件
- 数据库存储: 相对路径
- 文件命名: UUID + 原始扩展名

## 8. 配置文件

### 8.1 application.yml
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dance_studio?useSSL=false&serverTimezone=UTC
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

jwt:
  secret: your-secret-key
  expiration: 86400000
  refresh-expiration: 604800000

file:
  upload-dir: ./upload
```

## 9. API文档

### 9.1 Swagger配置
```java
@Configuration
@EnableSwagger2
public class SwaggerConfig {
    @Bean
    public Docket api() {
        return new Docket(DocumentationType.SWAGGER_2)
            .select()
            .apis(RequestHandlerSelectors.basePackage("com.example.backend.controller"))
            .paths(PathSelectors.any())
            .build()
            .apiInfo(apiInfo());
    }
}
```

### 9.2 API访问地址
- Swagger UI: `http://localhost:8080/swagger-ui.html`
- API Docs: `http://localhost:8080/v2/api-docs`

## 10. 性能优化

### 10.1 数据库优化
- 合理使用索引
- 查询优化
- 连接池配置
- 分页查询

### 10.2 缓存策略
- Spring Cache
- Redis缓存
- 查询结果缓存

### 10.3 异步处理
- 异步任务
- 定时任务
- 消息队列

## 11. 测试

### 11.1 单元测试
- Service层测试
- Repository层测试
- 工具类测试

### 11.2 集成测试
- Controller层测试
- 安全认证测试
- API接口测试

## 12. 部署

### 12.1 打包
```bash
mvn clean package
```

### 12.2 运行
```bash
java -jar backend.jar
```

### 12.3 环境配置
- 开发环境: `application-dev.yml`
- 生产环境: `application-prod.yml`

## 13. 日志

### 13.1 日志配置
```yaml
logging:
  level:
    root: INFO
    com.example.backend: DEBUG
  file:
    name: logs/application.log
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
```

### 13.2 日志级别
- ERROR: 错误日志
- WARN: 警告日志
- INFO: 信息日志
- DEBUG: 调试日志
