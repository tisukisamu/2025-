# 摘  要

随着人们生活水平的不断提高和宠物饲养观念的转变，宠物已经成为许多家庭的重要成员。然而，当宠物离世时，如何为它们提供体面、温馨的告别仪式成为了一个亟待解决的社会问题。传统的宠物殡葬服务存在信息不透明、流程不规范、缺乏情感关怀等问题，无法满足现代宠物主人对专业化、人性化服务的需求。为了解决这些问题，我设计并开发了一套完整的宠物殡葬管理系统，实现了从预约服务到流程追踪的全流程数字化管理，为宠物主人提供便捷、透明、温馨的服务体验。

本系统采用前后端分离的架构设计，前端使用Vue 3框架结合Vite构建工具、Ant Design Vue组件库和UnoCSS原子化CSS引擎，实现了响应式的用户界面；后端采用Spring Boot 3框架，结合Hibernate ORM框架和MySQL数据库，实现了稳定可靠的数据存储和业务逻辑处理。系统采用JWT（JSON Web Token）实现身份认证，保障了系统的安全性。系统主要功能包括：用户管理、宠物信息管理、服务套餐管理、预约管理、服务流程追踪、纪念相册、留言互动、支付管理等模块。通过标准化的流程设计和情感化的功能开发，实现了行业痛点与技术方案的精准对接，打造了兼具实用性与人文关怀的管理系统。

关键词：宠物殡葬；Spring Boot；Vue 3；流程追踪；情感化设计

---

# ABSTRACT

With the continuous improvement of people's living standards and the transformation of pet keeping concepts, pets have become important members of many families. However, when pets pass away, how to provide them with decent and warm farewell ceremonies has become an urgent social problem. Traditional pet funeral services suffer from problems such as opaque information, non-standard processes, and lack of emotional care, which cannot meet modern pet owners' demands for professional and humanized services. To solve these problems, I designed and developed a complete pet funeral management system, achieving full-process digital management from service reservation to process tracking, providing pet owners with convenient, transparent, and warm service experiences.

This system adopts a front-end and back-end separation architecture design. The front-end uses the Vue 3 framework combined with Vite build tool, Ant Design Vue component library, and UnoCSS atomic CSS engine to achieve a responsive user interface. The back-end adopts the Spring Boot 3 framework, combined with Hibernate ORM framework and MySQL database, achieving stable and reliable data storage and business logic processing. The system uses JWT (JSON Web Token) for identity authentication, ensuring system security. The main functions of the system include: user management, pet information management, service package management, appointment management, service process tracking, memorial album, message interaction, payment management and other modules. Through standardized process design and emotional function development, the precise docking of industry pain points and technical solutions has been achieved, creating a management system with both practicality and humanistic care.

Key words: Pet Funeral; Spring Boot; Vue 3; Process Tracking; Emotional Design

---

# 1 引言

## 1.1 研究背景及意义

随着我国经济社会的快速发展和人民生活水平的不断提高，宠物饲养已经成为越来越多家庭的选择。据统计，中国宠物市场规模已突破3000亿元，宠物数量超过1亿只，宠物经济呈现出蓬勃发展的态势。宠物不再仅仅是看家护院的工具，而是成为了许多家庭的重要成员，承载着主人的情感寄托和精神慰藉。宠物与主人之间建立起的深厚情感纽带，使得宠物离世时的告别仪式变得尤为重要。

然而，当前宠物殡葬行业的发展却相对滞后，存在诸多问题亟待解决。首先，服务信息不透明，宠物主人难以了解具体的服务内容、价格标准和操作流程，容易产生信任危机。其次，服务流程不规范，缺乏统一的行业标准和操作规范，服务质量参差不齐。再次，缺乏情感关怀，传统殡葬服务过于注重流程执行，忽视了对宠物主人情感需求的关注和疏导。最后，数字化程度低，大部分宠物殡葬机构仍采用传统的手工记录方式，效率低下且容易出错。

在互联网技术高速发展的背景下，基于B/S架构的Web应用技术得到了广泛的应用和推广。MVC设计模式在Web开发中的应用越来越成熟，通过分层设计的理念，实现了分层解耦、代码复用，更方便维护和测试软件系统的目的。前后端分离架构的流行，使得前端和后端可以独立开发、独立部署，大大提高了开发效率和系统的可维护性。响应式网页设计可以实现单个网站的页面兼容多种设备，从而成为跨终端适配的解决方案之一。

基于上述背景，本人研究并设计了一套宠物殡葬管理系统，旨在通过信息化手段解决当前宠物殡葬行业存在的问题。该系统以"技术架构+殡葬场景+情感关怀"为核心理念，通过标准化的流程设计、透明化的信息展示、情感化的功能开发，为宠物主人提供便捷、温馨、专业的服务体验。系统的开发不仅具有重要的实用价值，也具有深远的社会意义，体现了对生命的尊重和对人性的关怀。

一个完善的宠物殡葬管理系统无论从经济价值上，还是从社会价值来说都是非常有必要的。完整的服务流程管理意味着将在人力物力上节约成本，同时加快处理速度，提高用户体验。系统的应用将推动宠物殡葬行业的数字化转型，提升行业整体服务水平，满足人民群众日益增长的美好生活需要。

## 1.2 国内外发展现状及趋势

### 1.2.1 国内外发展现状

（1）国内发展现状

随着中国宠物市场的不断扩大，宠物殡葬服务也逐渐受到重视。目前，国内宠物殡葬服务主要集中在一线城市和部分二线城市，服务形式包括火化、安葬、纪念品制作等。然而，整体行业发展仍处于初级阶段，存在诸多不足。

在服务模式方面，国内宠物殡葬机构多采用线下门店经营模式，客户需要亲自到店咨询和办理业务，服务流程繁琐，效率低下。部分机构虽然开通了线上预约渠道，但功能单一，仅停留在简单的信息收集层面，无法提供完整的服务体验。在信息化建设方面，大多数机构缺乏专业的管理系统，仍采用传统的纸质记录方式，数据管理混乱，难以进行有效的业务分析和决策支持。在服务标准化方面，行业缺乏统一的服务标准和操作规范，不同机构的服务内容和质量差异较大，消费者权益难以得到保障。

近年来，随着互联网技术的发展，一些创新的宠物殡葬服务平台开始涌现。这些平台尝试通过互联网技术改善传统服务模式的不足，提供在线预约、进度查询、在线支付等功能。然而，这些平台大多功能不够完善，缺乏对服务全流程的深度整合，无法真正满足用户的多样化需求。特别是在情感关怀方面，现有系统普遍缺乏人性化的设计，无法为宠物主人提供心理支持和情感疏导。

（2）国外发展现状

在国外，特别是欧美和日本等发达国家，宠物殡葬服务行业发展相对成熟。这些国家不仅建立了完善的宠物殡葬服务体系，还制定了相应的法律法规和行业标准，保障了行业的健康发展。

在服务模式方面，国外宠物殡葬服务更加多样化和个性化。除了传统的火化和安葬服务外，还提供宠物遗体美容、告别仪式策划、纪念品定制、心理咨询等增值服务。许多机构还建立了宠物纪念公园和宠物公墓，为宠物主人提供追思和缅怀的场所。在信息化建设方面，国外宠物殡葬机构普遍采用专业的管理系统，实现了从预约到服务的全流程数字化管理。客户可以通过网站或移动应用完成预约、支付、进度查询等操作，服务体验良好。在情感关怀方面，国外机构非常重视对宠物主人的心理支持，配备专业的心理咨询师，提供悲伤辅导和情感疏导服务。

在技术应用方面，国外宠物殡葬服务积极拥抱新技术。例如，利用虚拟现实技术创建虚拟纪念空间，让宠物主人可以在虚拟环境中与逝去的宠物互动；利用区块链技术记录宠物生命历程，确保数据的真实性和不可篡改性；利用人工智能技术提供智能客服和情感陪伴服务。这些创新技术的应用，极大地丰富了宠物殡葬服务的内涵，提升了服务品质。

### 1.2.2 发展趋势

随着互联网信息技术的高速发展和宠物经济的持续升温，宠物殡葬服务行业正迎来前所未有的发展机遇。未来，宠物殡葬服务将呈现以下发展趋势：

首先，服务数字化将成为行业标配。通过建设专业的宠物殡葬管理系统，实现服务预约、流程追踪、在线支付、数据统计等功能的数字化，提高服务效率，降低运营成本。同时，数字化系统可以积累大量的用户数据和服务数据，为业务优化和决策支持提供数据基础。

其次，服务标准化将成为行业发展方向。制定统一的服务标准和操作规范，明确服务内容、服务流程、服务质量要求，保障消费者权益，促进行业健康发展。标准化建设需要行业协会、企业和政府部门的共同努力。

再次，情感化服务将成为核心竞争力。宠物殡葬服务不仅仅是处理宠物遗体，更重要的是为宠物主人提供情感支持和心理疏导。未来，宠物殡葬机构将更加注重情感化服务设计，提供告别仪式策划、纪念品定制、心理咨询等增值服务，满足宠物主人的情感需求。

最后，技术创新将推动行业变革。虚拟现实、人工智能、区块链等新技术的应用，将为宠物殡葬服务带来全新的体验。例如，利用虚拟现实技术创建虚拟纪念空间，利用人工智能技术提供智能客服和情感陪伴，利用区块链技术记录宠物生命历程等。这些创新技术的应用，将极大地丰富宠物殡葬服务的内涵，提升服务品质。

## 1.3 论文组织结构

本文共六章，论文的组织结构及各章节介绍如下：

第一章为引言，主要介绍研究背景及意义、国内外发展现状及趋势，阐述宠物殡葬管理系统开发的必要性和可行性；

第二章为系统需求分析，主要从经济可行性、技术可行性、操作可行性三个方面进行可行性分析，并通过业务流分析、数据流分析、数据字典等方法详细分析系统需求；

第三章为系统概要设计，主要介绍系统总体设计结构、数据库表设计、接口设计和系统出错处理设计，为系统详细设计奠定基础；

第四章为系统详细设计，主要介绍系统登录模块、用户操作模块、预约管理模块、流程追踪模块等核心功能模块的设计实现，包括模块流程图、界面展示和核心代码；

第五章为系统测试，主要介绍系统测试概述、系统测试方法和系统测试用例，验证系统功能的正确性和稳定性；

第六章为结论与展望，总结系统开发过程中的收获和不足，展望系统未来的发展方向。

本次毕业设计中，根据需求分析、概要设计、详细设计以及系统测试流程进行设计，这一过程大大提升了个人分析与设计能力。

---

# 2 系统需求分析

## 2.1 可行性分析

在软件开发过程中对软件进行可行性分析是一个重要步骤。可行性分析主要从经济、技术、操作等方面分析所给出的解决方案是否可行，软件的可行性分析是通过对项目的市场需求、环境影响盈利能力等方面的研究分析。

### 2.1.1 经济可行性

软件的经济可行性意味着有没有钱来完成开发，同时也要分析这个软件能否给用户带来实际经济效益。本系统是为了宠物主人可以更加方便地获得宠物殡葬服务，是个人独立研究设计，只是消耗时间成本，并不需要其他太多的消费。从市场需求来看，中国宠物市场规模已突破3000亿元，宠物数量超过1亿只，宠物殡葬服务需求巨大。从经济效益来看，系统可以帮助宠物殡葬机构提高服务效率，降低运营成本，增加收入来源。对于宠物主人而言，系统提供了便捷的服务预约和透明的价格信息，节省了时间和精力成本。因此，本系统的开发设计在经济上是可行的。

### 2.1.2 技术可行性

本系统主要是开发宠物殡葬管理系统，只要有宠物殡葬服务资源，就可以根据用户的需求来提供服务。另一方面，本系统采用目前主流开源的开发工具和熟悉的开发框架进行开发的，不需要太复杂的技术，根据自己所学就可以开发本系统及解决本系统中遇到的技术难点。

前端技术方面，系统采用Vue 3框架，这是目前最流行的前端框架之一，具有组件化、响应式、虚拟DOM等特点，开发效率高，性能优越。Vite作为新一代前端构建工具，具有快速的开发服务器启动和热更新能力，大大提高了开发效率。Ant Design Vue是基于Ant Design设计体系的Vue组件库，提供了丰富的企业级UI组件，可以快速构建美观、专业的用户界面。UnoCSS是即时原子化CSS引擎，具有高性能、灵活性强等特点，可以有效管理样式代码。

后端技术方面，系统采用Spring Boot 3框架，这是目前最流行的Java Web开发框架，具有自动配置、开箱即用、约定优于配置等特点，可以快速搭建企业级应用。Hibernate是最流行的ORM框架之一，可以实现对象关系映射，简化数据库操作。MySQL是最流行的开源关系型数据库，具有性能优越、稳定可靠、成本低廉等特点。JWT（JSON Web Token）是目前最流行的身份认证方案之一，具有无状态、可扩展性好等特点。

因此，开发本系统在技术上是可行的。

### 2.1.3 操作可行性

本系统是所有互联网用户都可以使用，界面简单明了。用户可以根据自己的需求在软件上操作就可以完成个人所需，操作流程简单。用户需要注册账号并登录后才能使用系统的完整功能，注册过程简单快捷，只需要填写基本的个人信息即可。登录后，用户可以管理自己的宠物信息、预约服务、查看服务进度、创建纪念相册等。管理员可以通过管理后台对用户、宠物、服务、预约等进行管理，操作界面友好，功能清晰明了。通过用户使用手册就可以完成软件内的基本功能。因此，开发本系统在操作上是可行的。

## 2.2 系统需求分析

### 2.2.1 业务流分析

宠物殡葬管理系统用户通过注册登录使用。未登录时只能浏览服务套餐信息，不能进行预约等相关操作。当用户注册登录之后就可以对宠物信息进行管理、预约服务、查看服务进度、创建纪念相册等操作。管理员可以通过管理后台对用户、宠物、服务套餐、预约、服务流程等进行管理操作。服务人员可以查看分配给自己的任务，更新服务进度，上传服务记录等。操作过程的数据都将保存到数据库中。宠物殡葬管理系统业务流图如图2-1所示。

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户注册/登录                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        用户操作                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ 宠物管理  │  │ 服务预约  │  │ 进度查询  │  │ 纪念相册  │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        管理员操作                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ 用户管理  │  │ 服务管理  │  │ 预约管理  │  │ 统计分析  │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        服务人员操作                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                      │
│  │ 任务列表  │  │ 进度更新  │  │ 记录上传  │                      │
│  └──────────┘  └──────────┘  └──────────┘                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        数据库存储                                 │
└─────────────────────────────────────────────────────────────────┘
```

图 2-1 宠物殡葬管理系统业务流图

### 2.2.2 数据流分析

宠物殡葬管理系统的数据流是依赖用户操作前端应用来进行流向的，用户在前端界面的操作就是向系统输入数据，系统处理后返回相应的结果数据。这种数据的流向是双向同步的，只有用户进行操作，才会输出相应的数据。宠物殡葬管理系统数据流图如图2-2所示。

```
┌──────────┐                    ┌──────────┐
│  前端应用 │                    │  后端服务 │
└────┬─────┘                    └────┬─────┘
     │                               │
     │  1. 用户请求                   │
     │─────────────────────────────▶│
     │                               │
     │                               │ 2. 业务处理
     │                               │
     │                               ▼
     │                          ┌──────────┐
     │                          │ 数据库   │
     │                          │ MySQL    │
     │                          └────┬─────┘
     │                               │
     │                               │ 3. 数据返回
     │                               │
     │  4. 响应数据                   │
     │◀─────────────────────────────│
     │                               │
     │                               │
     │  5. 文件上传                   │
     │─────────────────────────────▶│
     │                               │
     │                               ▼
     │                          ┌──────────┐
     │                          │ 文件存储 │
     │                          │ upload/  │
     │                          └──────────┘
     │                               │
     │  6. 返回文件路径               │
     │◀─────────────────────────────│
     │                               │
```

图 2-2 宠物殡葬管理系统数据流图

### 2.2.3 数据字典

数据字典是一种通用的程序设计方法，数据字典标准是以概念数据模型为基础，提供基础数据集的空间与层次要素的标准定义，如数据存储、数据处理逻辑、数据流、数据结构、外部实体等。其目的是对数据流程图中的各个元素做出详细的说明。数据字典是描述数据的信息集合，是一种用户可以访问的记录数据库和应用程序元数据的目录。

宠物殡葬管理系统的用户数据字典，是由用户详细信息组成的，这些详细信息保存在数据库中。如表2-1所示。

表2-1 用户数据模型表

| 数据项存储名 | 用户信息 |
|------------|---------|
| 数据项来源 | 用户信息表 |
| 数据项去向 | 用户登录模块、用户管理模块 |
| 数据项组成 | 用户信息=用户ID+用户名+密码+手机号+邮箱+真实姓名+头像+角色+状态+创建时间+更新时间 |
| 数据项描述 | 保存了用户的基本信息和账户状态 |

宠物殡葬管理系统的宠物数据字典，如表2-2所示。

表2-2 宠物数据模型表

| 数据项存储名 | 宠物信息 |
|------------|---------|
| 数据项来源 | 宠物信息表 |
| 数据项去向 | 宠物管理模块、预约服务模块 |
| 数据项组成 | 宠物信息=宠物ID+用户ID+宠物名称+宠物类型+品种+性别+出生日期+去世日期+照片+毛色+体重+描述+纪念文字+创建时间+更新时间 |
| 数据项描述 | 保存了宠物的基本信息和生命历程 |

宠物殡葬管理系统的预约数据字典，如表2-3所示。

表2-3 预约数据模型表

| 数据项存储名 | 预约信息 |
|------------|---------|
| 数据项来源 | 预约表 |
| 数据项去向 | 预约管理模块、服务流程模块 |
| 数据项组成 | 预约信息=预约ID+订单号+用户ID+宠物ID+套餐ID+预约时间+联系人+联系电话+服务地址+备注+状态+服务人员ID+创建时间+更新时间 |
| 数据项描述 | 保存了用户的预约信息和服务状态 |

### 2.2.4 数据库需求分析

宠物殡葬管理系统使用了MySQL数据库，而MySQL所使用的SQL语言是用于访问数据库的最常用标准化语言。MySQL是一种关系数据库管理系统，关系数据库将数据保存在不同的表中，增加了速度并提高了灵活性。对于个人使用者来讲，MySQL提供的功能已经足够了。

通过对本系统的功能需求分析和数据库需求分析，宠物殡葬管理系统有如下功能需求信息：

管理员功能需求分析：
(1) 用户管理：管理员可以对用户进行增删改查操作，包括查看用户列表、编辑用户信息、禁用/启用用户账号等；
(2) 宠物管理：管理员可以查看所有宠物信息，对违规信息进行处理；
(3) 服务管理：管理员可以管理服务套餐，包括添加、编辑、删除服务套餐，设置价格和状态；
(4) 预约管理：管理员可以查看所有预约订单，进行审核、分配服务人员、更新状态等操作；
(5) 流程管理：管理员可以查看服务流程进度，对异常情况进行处理；
(6) 统计分析：管理员可以查看服务统计、用户统计、收入统计等数据报表。

用户功能需求分析：
(1) 用户注册：新用户可以注册账号，填写基本信息；
(2) 用户登录：已注册用户可以登录系统；
(3) 个人信息管理：用户可以查看和编辑自己的个人信息；
(4) 宠物管理：用户可以添加、编辑、删除自己的宠物信息；
(5) 服务预约：用户可以选择服务套餐，填写预约信息，提交预约申请；
(6) 进度查询：用户可以查看自己预约的服务进度，了解服务执行情况；
(7) 纪念相册：用户可以为逝去的宠物创建纪念相册，上传照片，撰写纪念文字；
(8) 留言互动：用户可以在纪念相册中发表留言，表达对宠物的思念。

服务人员功能需求分析：
(1) 任务列表：服务人员可以查看分配给自己的任务列表；
(2) 任务详情：服务人员可以查看任务的详细信息；
(3) 进度更新：服务人员可以更新服务进度，上传服务记录（照片、视频等）；
(4) 服务记录：服务人员可以查看自己的服务历史记录。

---

# 3 系统概要设计

## 3.1 系统总体设计结构

宠物殡葬管理系统总体结构设计功能图如下所示。

```
┌─────────────────────────────────────────────────────────────────┐
│                      宠物殡葬管理系统                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │  用户管理   │  │  宠物管理   │  │  服务管理   │            │
│  ├─────────────┤  ├─────────────┤  ├─────────────┤            │
│  │·用户注册    │  │·宠物档案    │  │·服务套餐    │            │
│  │·用户登录    │  │·宠物信息    │  │·价格管理    │            │
│  │·个人信息    │  │·健康记录    │  │·服务类型    │            │
│  │·角色权限    │  │·生命历程    │  │·增值服务    │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │  预约管理   │  │  流程追踪   │  │  情感关怀   │            │
│  ├─────────────┤  ├─────────────┤  ├─────────────┤            │
│  │·在线预约    │  │·进度查询    │  │·纪念相册    │            │
│  │·预约审核    │  │·节点通知    │  │·追思留言    │            │
│  │·时间安排    │  │·流程记录    │  │·情感引导    │            │
│  │·取消改约    │  │·透明展示    │  │·心理支持    │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │  文件管理   │  │  支付管理   │  │  统计分析   │            │
│  ├─────────────┤  ├─────────────┤  ├─────────────┤            │
│  │·图片上传    │  │·订单管理    │  │·服务统计    │            │
│  │·文档存储    │  │·支付接口    │  │·用户分析    │            │
│  │·证书生成    │  │·退款处理    │  │·收入报表    │            │
│  │·资料归档    │  │·发票管理    │  │·趋势分析    │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

图3-1 宠物殡葬管理系统总体结构设计功能图

## 3.2 数据库表设计

### 3.2.1 ER图

宠物殡葬管理系统的E-R图如下所示。

```
┌─────────┐         ┌─────────┐         ┌─────────┐
│  用户   │         │  宠物   │         │服务套餐 │
├─────────┤         ├─────────┤         ├─────────┤
│用户ID(PK)│────────│宠物ID(PK)│        │套餐ID(PK)│
│用户名    │    1:n │用户ID(FK)│        │套餐名称  │
│密码      │         │宠物名称  │         │套餐类型  │
│手机号    │         │宠物类型  │         │价格      │
│邮箱      │         │品种      │         │描述      │
│角色      │         │性别      │         │状态      │
│状态      │         │出生日期  │         └────┬────┘
└────┬────┘         │去世日期  │              │
     │              │照片      │              │
     │              └────┬────┘              │
     │                   │                   │
     │              ┌────┴────┐              │
     │              │         │              │
     │         ┌────┴─────────┴────┐         │
     │         │      预约         │         │
     │         ├───────────────────┤         │
     │         │预约ID(PK)         │         │
     ├────────▶│用户ID(FK)         │◀────────┤
     │         │宠物ID(FK)         │         │
     │         │套餐ID(FK)         │         │
     │         │订单号             │         │
     │         │预约时间           │         │
     │         │联系人             │         │
     │         │联系电话           │         │
     │         │服务地址           │         │
     │         │状态               │         │
     │         └────────┬──────────┘         │
     │                  │                    │
     │         ┌────────┴────────┐           │
     │         │   服务流程      │           │
     │         ├─────────────────┤           │
     │         │流程ID(PK)       │           │
     │         │预约ID(FK)       │           │
     │         │阶段             │           │
     │         │状态             │           │
     │         │操作人员ID(FK)───┼───────────┘
     │         │描述             │
     │         │照片             │
     │         │视频             │
     │         └─────────────────┘
     │
     │         ┌─────────────────┐
     │         │   纪念相册      │
     │         ├─────────────────┤
     │         │相册ID(PK)       │
     ├────────▶│用户ID(FK)       │
     │         │宠物ID(FK)       │
     │         │标题             │
     │         │描述             │
     │         │照片             │
     │         │是否公开         │
     │         └────────┬────────┘
     │                  │
     │         ┌────────┴────────┐
     │         │     留言        │
     │         ├─────────────────┤
     │         │留言ID(PK)       │
     ├────────▶│用户ID(FK)       │
               │相册ID(FK)       │
               │留言者姓名       │
               │留言内容         │
               └─────────────────┘
```

图3-2 宠物殡葬管理系统的E-R图

### 3.2.2 数据表设计

本系统在设计数据库时首先根据需求绘制E-R图，再将E-R图转换为多张表，系统数据库名pet_memorial，其表设计分别如下所示。

（1）用户表设计，如表3-1所示。

表3-1 用户表(sys_user)

| 字段名 | 类型 | 是否为空 | 描述 |
|--------|------|----------|------|
| id | BIGINT | NOT NULL | 用户ID，主键，自增 |
| username | VARCHAR(50) | NOT NULL | 用户名，唯一 |
| password | VARCHAR(255) | NOT NULL | 密码（加密存储） |
| phone | VARCHAR(20) | NULL | 手机号，唯一 |
| email | VARCHAR(100) | NULL | 邮箱 |
| real_name | VARCHAR(50) | NULL | 真实姓名 |
| avatar | VARCHAR(255) | NULL | 头像URL |
| role | VARCHAR(20) | NOT NULL | 角色：user-普通用户，service-服务人员，admin-管理员 |
| status | TINYINT | NOT NULL | 状态：0-禁用，1-启用 |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |
| deleted | TINYINT | NOT NULL | 删除标记：0-未删除，1-已删除 |

（2）宠物信息表设计，如表3-2所示。

表3-2 宠物信息表(pet_info)

| 字段名 | 类型 | 是否为空 | 描述 |
|--------|------|----------|------|
| id | BIGINT | NOT NULL | 宠物ID，主键，自增 |
| user_id | BIGINT | NOT NULL | 用户ID，外键 |
| name | VARCHAR(50) | NOT NULL | 宠物名称 |
| type | VARCHAR(20) | NOT NULL | 宠物类型：dog-狗，cat-猫，other-其他 |
| breed | VARCHAR(50) | NULL | 品种 |
| gender | VARCHAR(10) | NULL | 性别：male-雄性，female-雌性 |
| birthday | DATE | NULL | 出生日期 |
| pass_date | DATE | NULL | 去世日期 |
| photo | VARCHAR(255) | NULL | 宠物照片URL |
| color | VARCHAR(20) | NULL | 毛色 |
| weight | DECIMAL(5,2) | NULL | 体重(kg) |
| description | TEXT | NULL | 描述 |
| memorial_text | TEXT | NULL | 纪念文字 |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |
| deleted | TINYINT | NOT NULL | 删除标记 |

（3）服务套餐表设计，如表3-3所示。

表3-3 服务套餐表(service_package)

| 字段名 | 类型 | 是否为空 | 描述 |
|--------|------|----------|------|
| id | BIGINT | NOT NULL | 套餐ID，主键，自增 |
| name | VARCHAR(100) | NOT NULL | 套餐名称 |
| type | VARCHAR(20) | NOT NULL | 服务类型：cremation-火化，burial-安葬，memorial-纪念 |
| description | TEXT | NULL | 套餐描述 |
| price | DECIMAL(10,2) | NOT NULL | 价格 |
| original_price | DECIMAL(10,2) | NULL | 原价 |
| includes | TEXT | NULL | 包含服务(JSON) |
| image | VARCHAR(255) | NULL | 套餐图片URL |
| sort_order | INT | NULL | 排序 |
| status | TINYINT | NOT NULL | 状态：0-下架，1-上架 |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |
| deleted | TINYINT | NOT NULL | 删除标记 |

（4）预约表设计，如表3-4所示。

表3-4 预约表(appointment)

| 字段名 | 类型 | 是否为空 | 描述 |
|--------|------|----------|------|
| id | BIGINT | NOT NULL | 预约ID，主键，自增 |
| order_no | VARCHAR(50) | NOT NULL | 订单号，唯一 |
| user_id | BIGINT | NOT NULL | 用户ID，外键 |
| pet_id | BIGINT | NOT NULL | 宠物ID，外键 |
| package_id | BIGINT | NOT NULL | 套餐ID，外键 |
| appointment_time | DATETIME | NOT NULL | 预约时间 |
| contact_name | VARCHAR(50) | NOT NULL | 联系人 |
| contact_phone | VARCHAR(20) | NOT NULL | 联系电话 |
| address | VARCHAR(255) | NULL | 服务地址 |
| remark | TEXT | NULL | 备注 |
| status | VARCHAR(20) | NOT NULL | 状态：pending-待审核，confirmed-已确认，processing-进行中，completed-已完成，cancelled-已取消 |
| operator_id | BIGINT | NULL | 服务人员ID，外键 |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |
| deleted | TINYINT | NOT NULL | 删除标记 |

（5）服务流程表设计，如表3-5所示。

表3-5 服务流程表(service_process)

| 字段名 | 类型 | 是否为空 | 描述 |
|--------|------|----------|------|
| id | BIGINT | NOT NULL | 流程ID，主键，自增 |
| appointment_id | BIGINT | NOT NULL | 预约ID，外键 |
| stage | VARCHAR(30) | NOT NULL | 阶段：confirmed-确认预约，pickup-接送宠物，farewell-告别仪式，cremation-火化服务，processing-骨灰处理，memorial-纪念品制作，completed-服务完成 |
| status | VARCHAR(20) | NOT NULL | 状态：pending-待处理，processing-进行中，completed-已完成 |
| operator_id | BIGINT | NULL | 操作人员ID，外键 |
| description | TEXT | NULL | 描述 |
| photos | TEXT | NULL | 照片(JSON数组) |
| videos | TEXT | NULL | 视频(JSON数组) |
| start_time | DATETIME | NULL | 开始时间 |
| end_time | DATETIME | NULL | 结束时间 |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |

（6）纪念相册表设计，如表3-6所示。

表3-6 纪念相册表(memorial_album)

| 字段名 | 类型 | 是否为空 | 描述 |
|--------|------|----------|------|
| id | BIGINT | NOT NULL | 相册ID，主键，自增 |
| pet_id | BIGINT | NOT NULL | 宠物ID，外键 |
| user_id | BIGINT | NOT NULL | 用户ID，外键 |
| title | VARCHAR(100) | NULL | 相册标题 |
| description | TEXT | NULL | 相册描述 |
| photos | TEXT | NULL | 照片(JSON数组) |
| is_public | TINYINT | NOT NULL | 是否公开：0-私密，1-公开 |
| view_count | INT | NOT NULL | 浏览次数 |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |
| deleted | TINYINT | NOT NULL | 删除标记 |

（7）留言表设计，如表3-7所示。

表3-7 留言表(message)

| 字段名 | 类型 | 是否为空 | 描述 |
|--------|------|----------|------|
| id | BIGINT | NOT NULL | 留言ID，主键，自增 |
| album_id | BIGINT | NOT NULL | 相册ID，外键 |
| user_id | BIGINT | NULL | 用户ID，外键（可为空，允许游客留言） |
| author_name | VARCHAR(50) | NOT NULL | 留言者姓名 |
| content | TEXT | NOT NULL | 留言内容 |
| create_time | DATETIME | NOT NULL | 创建时间 |
| deleted | TINYINT | NOT NULL | 删除标记 |

（8）支付订单表设计，如表3-8所示。

表3-8 支付订单表(payment_order)

| 字段名 | 类型 | 是否为空 | 描述 |
|--------|------|----------|------|
| id | BIGINT | NOT NULL | 订单ID，主键，自增 |
| order_no | VARCHAR(50) | NOT NULL | 订单号，唯一 |
| appointment_id | BIGINT | NOT NULL | 预约ID，外键 |
| user_id | BIGINT | NOT NULL | 用户ID，外键 |
| amount | DECIMAL(10,2) | NOT NULL | 支付金额 |
| payment_method | VARCHAR(20) | NULL | 支付方式：wechat-微信，alipay-支付宝 |
| transaction_id | VARCHAR(100) | NULL | 第三方交易号 |
| status | VARCHAR(20) | NOT NULL | 状态：pending-待支付，paid-已支付，failed-支付失败，refunded-已退款 |
| paid_time | DATETIME | NULL | 支付时间 |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |

（9）文件记录表设计，如表3-9所示。

表3-9 文件记录表(file_record)

| 字段名 | 类型 | 是否为空 | 描述 |
|--------|------|----------|------|
| id | BIGINT | NOT NULL | 文件ID，主键，自增 |
| original_name | VARCHAR(255) | NOT NULL | 原始文件名 |
| stored_name | VARCHAR(255) | NOT NULL | 存储文件名 |
| file_path | VARCHAR(500) | NOT NULL | 文件路径(相对路径) |
| file_size | BIGINT | NOT NULL | 文件大小(字节) |
| file_type | VARCHAR(50) | NOT NULL | 文件类型 |
| entity_type | VARCHAR(50) | NULL | 关联实体类型 |
| entity_id | BIGINT | NULL | 关联实体ID |
| uploader_id | BIGINT | NOT NULL | 上传者ID，外键 |
| create_time | DATETIME | NOT NULL | 创建时间 |

## 3.3 接口设计

### 3.3.1 用户接口设计

用户接口设计是系统与用户交互的桥梁，直接影响用户的使用体验和工作效率。本系统采用前后端分离架构，前端使用Vue 3框架结合Ant Design Vue组件库构建用户界面，后端使用Spring Boot框架提供RESTful API接口。系统根据用户角色的不同，设计了不同的功能模块和接口，满足不同用户群体的需求。

#### （1）功能模块设计

根据需求分析，系统设计了以下六大功能模块：

**① 用户认证模块**

用户认证模块是系统的入口模块，负责用户的注册、登录、身份验证和权限控制。该模块的设计目的是确保系统的安全性，防止未授权访问，同时为不同角色的用户提供个性化的服务入口。系统支持三种用户角色：普通用户（USER）、服务人员（SERVICE）和管理员（ADMIN），每种角色具有不同的权限和功能。

设计该模块的原因：宠物殡葬服务涉及用户的个人隐私和宠物信息，需要严格的身份认证机制保护用户数据安全。同时，不同角色的用户需要访问不同的功能模块，通过角色权限控制可以实现精细化的访问管理。

**② 宠物管理模块**

宠物管理模块允许用户添加、编辑、删除和查看宠物信息，记录宠物的基本信息、健康状况、生命历程等。该模块支持上传宠物照片，记录宠物的成长历程，为后续的纪念相册功能提供素材。

设计该模块的原因：宠物是宠物殡葬服务的核心对象，完整的宠物信息档案是提供个性化服务的基础。用户可以在系统中建立宠物的数字档案，记录宠物与主人之间的美好回忆，为后续的预约服务和纪念功能提供数据支持。

**③ 服务预约模块**

服务预约模块是系统的核心业务模块，提供服务套餐浏览、在线预约、预约管理等功能。用户可以浏览不同类型的服务套餐（基础套餐、标准套餐、尊享套餐），选择合适的套餐进行预约，填写预约信息，提交预约申请。

设计该模块的原因：传统的宠物殡葬服务需要用户亲自到店咨询和预约，流程繁琐，效率低下。通过在线预约模块，用户可以随时随地浏览服务套餐，了解服务内容和价格，自主选择合适的服务，大大提高了服务效率和用户体验。

**④ 流程追踪模块**

流程追踪模块实现了服务全流程的可视化追踪，将整个服务过程划分为确认预约、接送宠物、告别仪式、火化服务、骨灰处理、纪念品制作、服务完成七个阶段。用户可以实时查看服务进度，了解每个阶段的执行情况，查看服务人员上传的照片和视频。

设计该模块的原因：宠物殡葬服务是一个情感敏感的服务过程，宠物主人希望了解服务的每一个细节，确保宠物得到妥善对待。流程追踪模块实现了服务的透明化，让用户能够全程参与和监督服务过程，增强用户的信任感和满意度。

**⑤ 纪念相册模块**

纪念相册模块是系统的情感关怀特色模块，允许用户为逝去的宠物创建专属的纪念相册，上传宠物生前的照片，撰写纪念文字，记录宠物与主人之间的美好回忆。模块支持公开和私密两种访问模式，公开模式下可以分享给亲友，让更多人缅怀逝去的宠物。

设计该模块的原因：宠物对于主人来说不仅是动物，更是家庭成员和情感寄托。当宠物离世时，主人需要一个情感寄托的场所来缅怀和追思。纪念相册模块提供了这样的空间，让主人可以永久保存宠物的记忆，获得情感慰藉。

**⑥ 管理后台模块**

管理后台模块为管理员提供用户管理、服务管理、预约管理、统计分析等功能。管理员可以查看所有用户信息，对违规用户进行禁用或启用操作；管理服务套餐，添加、编辑、删除套餐信息；查看所有预约订单，进行审核、分配服务人员、更新状态等操作；查看数据统计报表，了解系统运营情况。

设计该模块的原因：系统需要一个统一的管理平台来维护系统的正常运转，管理用户、服务、预约等数据，监控系统的运行状态。管理后台模块提供了完整的管理功能，帮助管理员高效地管理整个系统。

#### （2）接口设计原则

系统接口设计遵循以下原则：

**① RESTful设计规范**

系统采用RESTful API设计规范，使用HTTP动词表示操作类型（GET表示查询，POST表示创建，PUT表示更新，DELETE表示删除），使用URL路径表示资源，使用HTTP状态码表示请求结果。这种设计风格简洁、清晰，易于理解和使用。

**② 统一响应格式**

所有接口返回统一的响应格式，包含code（状态码）、message（提示信息）和data（数据）三个字段。这种统一的格式方便前端统一处理响应，提高开发效率。

**③ 权限控制**

系统采用基于角色的权限控制（RBAC），不同角色的用户具有不同的访问权限。公开接口（如服务套餐列表、公开纪念相册）允许匿名访问；用户接口（如宠物管理、预约管理）需要USER角色；管理接口（如用户管理、服务管理）需要ADMIN角色。

**④ 版本控制**

接口路径包含版本号（如/api/v1/），方便后续接口升级和兼容性维护。当接口发生重大变更时，可以发布新版本的接口，同时保留旧版本接口，确保向后兼容。

#### （3）接口列表

系统设计了以下主要接口：

**① 用户认证接口**

| 接口路径 | HTTP方法 | 功能说明 | 权限要求 |
|---------|----------|----------|----------|
| /api/auth/register | POST | 用户注册 | 公开 |
| /api/auth/login | POST | 用户登录 | 公开 |
| /api/auth/logout | POST | 用户登出 | 需要认证 |
| /api/auth/profile | GET | 获取用户信息 | 需要认证 |
| /api/auth/profile | PUT | 更新用户信息 | 需要认证 |
| /api/auth/password | PUT | 修改密码 | 需要认证 |

**② 宠物管理接口**

| 接口路径 | HTTP方法 | 功能说明 | 权限要求 |
|---------|----------|----------|----------|
| /api/pets | GET | 获取宠物列表 | USER |
| /api/pets | POST | 添加宠物信息 | USER |
| /api/pets/{id} | GET | 获取宠物详情 | USER |
| /api/pets/{id} | PUT | 更新宠物信息 | USER |
| /api/pets/{id} | DELETE | 删除宠物信息 | USER |

**③ 服务套餐接口**

| 接口路径 | HTTP方法 | 功能说明 | 权限要求 |
|---------|----------|----------|----------|
| /api/services | GET | 获取服务套餐列表 | 公开 |
| /api/services/{id} | GET | 获取服务套餐详情 | 公开 |
| /api/services/type/{type} | GET | 按类型获取服务套餐 | 公开 |

**④ 预约管理接口**

| 接口路径 | HTTP方法 | 功能说明 | 权限要求 |
|---------|----------|----------|----------|
| /api/appointments | GET | 获取预约列表 | USER |
| /api/appointments | POST | 创建预约 | USER |
| /api/appointments/{id} | GET | 获取预约详情 | USER |
| /api/appointments/{id} | PUT | 更新预约信息 | USER |
| /api/appointments/{id} | DELETE | 取消预约 | USER |
| /api/appointments/{id}/process | GET | 获取服务进度 | USER |

**⑤ 流程追踪接口**

| 接口路径 | HTTP方法 | 功能说明 | 权限要求 |
|---------|----------|----------|----------|
| /api/process/appointment/{id} | GET | 获取预约的流程记录 | USER |
| /api/process/{id} | GET | 获取流程详情 | USER |
| /api/process/{id}/stage/{stage} | PUT | 更新流程阶段状态 | SERVICE |

**⑥ 纪念相册接口**

| 接口路径 | HTTP方法 | 功能说明 | 权限要求 |
|---------|----------|----------|----------|
| /api/memorials/public | GET | 获取公开纪念相册列表 | 公开 |
| /api/memorials | GET | 获取我的纪念相册列表 | USER |
| /api/memorials | POST | 创建纪念相册 | USER |
| /api/memorials/{id} | GET | 获取纪念相册详情 | 公开 |
| /api/memorials/{id} | PUT | 更新纪念相册 | USER |
| /api/memorials/{id} | DELETE | 删除纪念相册 | USER |

**⑦ 留言接口**

| 接口路径 | HTTP方法 | 功能说明 | 权限要求 |
|---------|----------|----------|----------|
| /api/messages/album/{albumId} | GET | 获取相册留言列表 | 公开 |
| /api/messages | POST | 发表留言 | 公开（游客可留言） |
| /api/messages/{id} | DELETE | 删除留言 | USER |

**⑧ 文件管理接口**

| 接口路径 | HTTP方法 | 功能说明 | 权限要求 |
|---------|----------|----------|----------|
| /api/files/upload | POST | 文件上传 | 需要认证 |
| /api/files/{path} | GET | 文件下载 | 公开 |
| /api/files/{id} | DELETE | 删除文件 | 需要认证 |

**⑨ 管理后台接口**

| 接口路径 | HTTP方法 | 功能说明 | 权限要求 |
|---------|----------|----------|----------|
| /api/admin/users | GET | 获取用户列表 | ADMIN |
| /api/admin/users/{id}/status | PUT | 更新用户状态 | ADMIN |
| /api/admin/services | GET | 获取所有服务套餐 | ADMIN |
| /api/admin/services | POST | 创建服务套餐 | ADMIN |
| /api/admin/services/{id} | PUT | 更新服务套餐 | ADMIN |
| /api/admin/services/{id} | DELETE | 删除服务套餐 | ADMIN |
| /api/admin/appointments | GET | 获取所有预约 | ADMIN |
| /api/admin/appointments/{id}/assign | PUT | 分配服务人员 | ADMIN |
| /api/admin/statistics | GET | 获取统计数据 | ADMIN |

#### （4）接口设计特点

**① 安全性**

系统采用JWT（JSON Web Token）进行身份认证，令牌中包含用户ID、用户名、角色等信息，后端通过验证令牌的有效性来确认用户身份。密码采用BCrypt算法加密存储，确保用户密码的安全性。敏感接口（如用户管理、服务管理）需要相应的角色权限才能访问。

**② 可扩展性**

接口设计采用模块化设计，每个功能模块独立设计接口，模块之间低耦合，方便后续功能扩展。接口路径包含版本号，支持多版本接口共存，方便接口升级和兼容性维护。

**③ 易用性**

接口设计遵循RESTful规范，接口路径清晰易懂，HTTP动词语义明确，响应格式统一，方便前端开发和第三方系统集成。接口提供详细的错误提示信息，帮助开发者快速定位问题。

**④ 性能优化**

接口支持分页查询，避免一次性返回大量数据；支持条件筛选和排序，提高查询效率；使用缓存机制缓存热点数据，减少数据库查询次数，提高系统性能。

### 3.3.2 外部接口设计

本系统需要与外部支付系统进行接口对接，实现支付功能。支付接口设计如下：

（1）微信支付接口
- 接口地址：由微信支付平台提供
- 请求方式：POST
- 请求参数：订单号、支付金额、商品描述、回调地址等
- 返回参数：支付二维码链接、预支付交易会话标识等

（2）支付宝支付接口
- 接口地址：由支付宝平台提供
- 请求方式：POST
- 请求参数：订单号、支付金额、商品描述、回调地址等
- 返回参数：支付表单、支付链接等

### 3.3.3 内部接口设计

系统内部接口采用RESTful API设计规范，接口设计如下：

（1）用户相关接口
- POST /api/v1/users/register：用户注册
- POST /api/v1/users/login：用户登录
- GET /api/v1/users/profile：获取用户信息
- PUT /api/v1/users/profile：更新用户信息

（2）宠物相关接口
- GET /api/v1/pets：获取宠物列表
- POST /api/v1/pets：添加宠物信息
- GET /api/v1/pets/{id}：获取宠物详情
- PUT /api/v1/pets/{id}：更新宠物信息
- DELETE /api/v1/pets/{id}：删除宠物信息

（3）预约相关接口
- POST /api/v1/appointments：创建预约
- GET /api/v1/appointments：获取预约列表
- GET /api/v1/appointments/{id}：获取预约详情
- PUT /api/v1/appointments/{id}/status：更新预约状态
- GET /api/v1/appointments/{id}/process：获取服务进度

（4）服务相关接口
- GET /api/v1/services：获取服务套餐列表
- GET /api/v1/services/{id}：获取服务套餐详情

（5）文件相关接口
- POST /api/v1/files/upload：文件上传
- GET /api/v1/files/{path}：文件下载
- DELETE /api/v1/files/{id}：文件删除

## 3.4 系统出错处理设计

本模块主要是针对出错的一些信息的处理方式，如表3-10所示。

表3-10 出错处理设计表

| 序号 | 出错名称 | 系统输出信息 | 处理方法 |
|------|----------|--------------|----------|
| 1 | 用户名输入错误 | "用户名不存在！请重新输入！" | 进入登录页面 |
| 2 | 密码输入错误 | "密码错误！请重新输入！" | 进入登录页面 |
| 3 | 密码错误次数超过三次 | "密码错误次数过多！请半小时后再进行登录操作！" | 进入默认登录页，并半小时内禁止该用户进行登录 |
| 4 | 输入数据格式错误 | "输入格式不正确，请检查后再输入！" | 返回原输入页面 |
| 5 | 必填项为空 | "请输入必填项！" | 弹回原输入页面的输入处 |
| 6 | 手机号格式错误 | "手机号格式不正确，请输入正确的手机号！" | 返回原输入页面 |
| 7 | 邮箱格式错误 | "邮箱格式不正确，请输入正确的邮箱！" | 返回原输入页面 |
| 8 | 文件上传失败 | "文件上传失败，请重试！" | 返回原上传页面 |
| 9 | 文件类型不支持 | "不支持该文件类型，请上传指定类型的文件！" | 返回原上传页面 |
| 10 | 文件大小超限 | "文件大小超过限制，请上传小于50MB的文件！" | 返回原上传页面 |
| 11 | 预约时间冲突 | "该时间段已有预约，请选择其他时间！" | 返回预约页面 |
| 12 | 服务套餐已下架 | "该服务套餐已下架，请选择其他套餐！" | 返回服务列表页面 |
| 13 | 权限不足 | "您没有权限执行此操作！" | 返回首页或上一页 |
| 14 | 系统故障 | "服务器维护中！暂停服务！" | 立即启用备用机，恢复故障 |
| 15 | 网络连接失败 | "网络连接失败，请检查网络设置！" | 提示用户检查网络 |

---

# 4 系统详细设计

## 4.1 系统登录模块

系统登录模块是整个系统的入口，承担着用户身份认证和权限控制的重要职责。该模块采用JWT（JSON Web Token）技术实现无状态的身份认证，确保系统的安全性和可扩展性。登录模块不仅需要验证用户的身份信息，还需要根据用户的角色类型（普通用户、服务人员、管理员）进行权限分配和页面跳转，为不同角色的用户提供个性化的服务入口。

在技术实现上，前端采用Vue 3的Composition API编写，使用Ant Design Vue组件库构建用户界面，通过响应式数据绑定实现表单验证和状态管理。后端采用Spring Security框架进行身份认证，使用BCrypt算法对密码进行加密存储，通过JWT生成和验证令牌，实现安全的无状态认证机制。登录成功后，系统会将JWT令牌存储在浏览器的LocalStorage中，后续请求通过Authorization请求头携带令牌，后端通过过滤器验证令牌的有效性。

### 4.1.1 模块流程图

系统登录模块的流程设计遵循安全认证的标准流程，从用户输入凭证开始，经过多重验证，最终完成身份认证和权限分配。整个流程包括输入验证、身份验证、状态检查、令牌生成和页面跳转五个主要环节，每个环节都有明确的验证逻辑和错误处理机制，确保系统的安全性和用户体验。系统登录实现流程图如下所示。

```
开始
  │
  ▼
输入用户名和密码
  │
  ▼
验证用户名和密码是否为空
  │
  ├─── 是 ──▶ 提示"请输入用户名和密码" ──▶ 返回登录页面
  │
  否
  │
  ▼
验证用户名是否存在
  │
  ├─── 否 ──▶ 提示"用户名不存在" ──▶ 返回登录页面
  │
  是
  │
  ▼
验证密码是否正确
  │
  ├─── 否 ──▶ 提示"密码错误" ──▶ 返回登录页面
  │
  是
  │
  ▼
验证账号状态是否正常
  │
  ├─── 否 ──▶ 提示"账号已被禁用" ──▶ 返回登录页面
  │
  是
  │
  ▼
生成JWT Token
  │
  ▼
保存Token到本地存储
  │
  ▼
根据用户角色跳转到对应页面
  │
  ├─── 管理员 ──▶ 跳转到管理后台
  ├─── 服务人员 ──▶ 跳转到服务端
  └─── 普通用户 ──▶ 跳转到用户首页
  │
  ▼
结束
```

图4-1 登录实现流程图

### 4.1.2 模块界面展示

系统登录页面采用简洁大方的设计风格，以白色为主色调，配合淡雅的背景色，营造专业、温馨的视觉氛围。页面中央是一个卡片式的登录表单，包含用户名输入框、密码输入框、登录按钮和注册链接。输入框采用大尺寸设计，方便用户输入，前方配有图标提示。登录按钮采用主题色，宽度占满整个表单，突出主要操作。整体布局居中对齐，视觉焦点明确，符合现代Web应用的设计规范。系统登录页面如下图所示。

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                    宠物殡葬管理系统                          │
│                                                             │
│              ┌─────────────────────────────┐               │
│              │                             │               │
│              │      用户名: [__________]   │               │
│              │                             │               │
│              │      密  码: [__________]   │               │
│              │                             │               │
│              │      [ 记住我 ]  [ 忘记密码? ]              │
│              │                             │               │
│              │         [ 登 录 ]           │               │
│              │                             │               │
│              │    还没有账号？立即注册      │               │
│              │                             │               │
│              └─────────────────────────────┘               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

图4-2 系统登录页面图

### 4.1.3 核心代码展示

登录模块的前端实现采用Vue 3的Composition API，使用`<script setup>`语法糖简化代码编写。表单验证使用Ant Design Vue提供的表单组件，通过rules属性定义验证规则，实现实时的表单验证。登录成功后，使用Pinia状态管理库存储用户信息和JWT令牌，并通过Vue Router进行页面跳转。前端代码采用TypeScript编写，提供类型安全，减少运行时错误。

登录模块前端核心代码：

```vue
<template>
  <div class="login-container">
    <div class="login-box">
      <h2 class="login-title">宠物殡葬管理系统</h2>
      <a-form
        :model="loginForm"
        :rules="rules"
        @finish="handleLogin"
      >
        <a-form-item name="username">
          <a-input
            v-model:value="loginForm.username"
            placeholder="请输入用户名"
            size="large"
          >
            <template #prefix>
              <UserOutlined />
            </template>
          </a-input>
        </a-form-item>
        
        <a-form-item name="password">
          <a-input-password
            v-model:value="loginForm.password"
            placeholder="请输入密码"
            size="large"
          >
            <template #prefix>
              <LockOutlined />
            </template>
          </a-input-password>
        </a-form-item>
        
        <a-form-item>
          <a-button
            type="primary"
            html-type="submit"
            size="large"
            block
            :loading="loading"
          >
            登录
          </a-button>
        </a-form-item>
        
        <div class="login-footer">
          还没有账号？<router-link to="/register">立即注册</router-link>
        </div>
      </a-form>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { message } from 'ant-design-vue'
import { UserOutlined, LockOutlined } from '@ant-design/icons-vue'
import { useUserStore } from '@/stores/user'

const router = useRouter()
const userStore = useUserStore()

const loginForm = ref({
  username: '',
  password: ''
})

const loading = ref(false)

const rules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 6, message: '密码长度不能少于6位', trigger: 'blur' }
  ]
}

async function handleLogin() {
  loading.value = true
  try {
    await userStore.loginAction(loginForm.value)
    message.success('登录成功')
    
    if (userStore.role === 'admin') {
      router.push('/admin')
    } else if (userStore.role === 'service') {
      router.push('/service/tasks')
    } else {
      router.push('/')
    }
  } catch (error: any) {
    message.error(error.message || '登录失败')
  } finally {
    loading.value = false
  }
}
</script>
```

登录模块后端核心代码：

后端登录接口使用Spring Security框架进行身份认证，通过AuthenticationManager验证用户凭证。认证成功后，使用JwtTokenProvider生成JWT令牌，令牌中包含用户ID、用户名、角色等信息。密码采用BCrypt算法加密存储，确保用户密码的安全性。登录接口返回JWT令牌和用户基本信息，前端保存令牌后，后续请求通过Authorization请求头携带令牌进行身份验证。

```java
@RestController
@RequestMapping("/v1/users")
@RequiredArgsConstructor
public class UserController {
    
    private final UserService userService;
    private final AuthenticationManager authenticationManager;
    private final JwtTokenProvider tokenProvider;
    
    @PostMapping("/login")
    public Result<Map<String, Object>> login(@Valid @RequestBody LoginRequest request) {
        Authentication authentication = authenticationManager.authenticate(
                new UsernamePasswordAuthenticationToken(
                        request.getUsername(),
                        request.getPassword()
                )
        );
        
        SecurityContextHolder.getContext().setAuthentication(authentication);
        String token = tokenProvider.generateToken(authentication);
        
        User user = userService.getUserByUsername(request.getUsername());
        
        Map<String, Object> result = new HashMap<>();
        result.put("token", token);
        result.put("user", convertToResponse(user));
        
        return Result.success("登录成功", result);
    }
    
    private UserResponse convertToResponse(User user) {
        UserResponse response = new UserResponse();
        response.setId(user.getId());
        response.setUsername(user.getUsername());
        response.setPhone(user.getPhone());
        response.setEmail(user.getEmail());
        response.setRealName(user.getRealName());
        response.setAvatar(user.getAvatar());
        response.setRole(user.getRole());
        response.setCreateTime(user.getCreateTime());
        return response;
    }
}
```

## 4.2 用户操作模块

用户操作模块是系统的核心功能模块，为普通用户提供宠物管理、服务预约、进度查询、纪念相册等主要功能。该模块采用分步骤引导式的设计理念，将复杂的预约流程分解为多个简单的步骤，降低用户的操作难度，提升用户体验。模块设计充分考虑了宠物殡葬服务的特殊性，在功能实现中融入了情感关怀元素，体现对生命的尊重。

服务预约功能是用户操作模块的核心，采用四步式流程设计：选择服务套餐、选择宠物、填写预约信息、确认提交。每个步骤都有清晰的界面引导和实时验证，确保用户能够顺利完成预约。系统支持多种服务套餐，包括基础套餐、标准套餐、尊享套餐等，每个套餐提供不同的服务内容和价格选择。用户可以根据自己的需求和经济能力选择合适的套餐，系统会实时显示套餐的详细信息、包含的服务项目和价格。

宠物管理功能允许用户添加、编辑、删除宠物信息，记录宠物的基本信息、健康状况、生命历程等。系统支持上传宠物照片，记录宠物的成长历程，为后续的纪念相册功能提供素材。进度查询功能让用户可以实时查看服务进度，了解每个阶段的执行情况，包括服务人员的操作记录、上传的照片和视频等，实现服务流程的透明化。

### 4.2.1 模块实现流程图

用户操作模块主要包括宠物管理、服务预约、进度查询、纪念相册等功能。以服务预约为例，该流程采用分步骤引导式设计，从用户选择服务套餐开始，依次完成宠物选择、信息填写、信息确认等步骤，最终提交预约申请。每个步骤都有明确的输入要求和验证规则，确保用户能够顺利完成预约操作。流程设计充分考虑了用户体验，支持返回上一步修改信息，降低了操作失误的风险。其流程图如下所示。

```
开始
  │
  ▼
用户选择服务套餐
  │
  ▼
填写预约信息
  │
  ├─── 宠物信息 ──▶ 选择已有宠物或添加新宠物
  ├─── 预约时间 ──▶ 选择预约日期和时间
  ├─── 联系信息 ──▶ 填写联系人和联系电话
  └─── 服务地址 ──▶ 填写服务地址
  │
  ▼
验证预约信息是否完整
  │
  ├─── 否 ──▶ 提示"请完善预约信息" ──▶ 返回填写页面
  │
  是
  │
  ▼
提交预约申请
  │
  ▼
生成预约订单
  │
  ▼
初始化服务流程
  │
  ▼
等待管理员审核
  │
  ▼
结束
```

图4-3 服务预约流程图

### 4.2.2 模块界面展示

服务预约页面采用分区域布局设计，顶部显示步骤进度条，清晰展示当前所处的预约阶段。页面主体分为三个主要区域：服务套餐选择区、宠物选择区和预约信息填写区。服务套餐以卡片形式展示，每个卡片包含套餐图片、名称、价格和简要描述，用户点击卡片即可选择。宠物选择区以单选列表形式展示用户已添加的宠物，支持快速添加新宠物。预约信息填写区采用表单布局，包含预约时间选择器、联系人输入框、联系电话输入框、服务地址输入框和备注文本域，所有必填项都有明确的标识。底部放置取消和提交按钮，操作按钮采用不同的颜色区分主次操作。服务预约页面如下图所示。

```
┌─────────────────────────────────────────────────────────────┐
│  预约服务                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  选择服务套餐                                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│  │ 基础套餐  │  │ 标准套餐  │  │ 尊享套餐  │                 │
│  │  ¥999    │  │ ¥1999    │  │ ¥3999    │                 │
│  │ [选择]   │  │ [选择]   │  │ [选择]   │                 │
│  └──────────┘  └──────────┘  └──────────┘                 │
│                                                             │
│  选择宠物                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ○ 小白 (狗 - 萨摩耶)                                │   │
│  │ ○ 小花 (猫 - 英短)                                  │   │
│  │ ○ 添加新宠物                                        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  预约信息                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 预约时间: [2024-01-15 10:00]                        │   │
│  │ 联系人:   [张三________________________________]    │   │
│  │ 联系电话: [13800138000_______________________]      │   │
│  │ 服务地址: [北京市朝阳区_______________________]      │   │
│  │ 备    注: [___________________________________]      │   │
│  │           [___________________________________]      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│                    [ 取消 ]      [ 提交预约 ]               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

图4-4 服务预约页面图

### 4.2.3 核心代码展示

服务预约模块的前端实现采用分步骤表单设计，使用Ant Design Vue的Steps组件展示预约进度。每个步骤对应一个表单页面，通过currentStep状态控制当前显示的步骤。用户完成一个步骤后，点击"下一步"按钮进入下一个步骤，支持返回上一步修改信息。最后一步展示所有填写的信息供用户确认，确认无误后提交预约申请。前端使用async/await处理异步请求，通过try-catch捕获异常并显示友好的错误提示。

服务预约模块前端核心代码：

```vue
<template>
  <div class="appointment-page">
    <h2>预约服务</h2>
    
    <a-steps :current="currentStep" class="steps">
      <a-step title="选择套餐" />
      <a-step title="选择宠物" />
      <a-step title="填写信息" />
      <a-step title="确认提交" />
    </a-steps>
    
    <div class="step-content">
      <div v-if="currentStep === 0" class="package-list">
        <a-row :gutter="16">
          <a-col :span="8" v-for="pkg in packages" :key="pkg.id">
            <a-card
              :class="{ 'selected': selectedPackage?.id === pkg.id }"
              hoverable
              @click="selectPackage(pkg)"
            >
              <img :src="pkg.image" :alt="pkg.name" />
              <h3>{{ pkg.name }}</h3>
              <p class="price">¥{{ pkg.price }}</p>
              <p class="description">{{ pkg.description }}</p>
            </a-card>
          </a-col>
        </a-row>
      </div>
      
      <div v-else-if="currentStep === 1" class="pet-list">
        <a-radio-group v-model:value="selectedPetId">
          <a-radio
            v-for="pet in pets"
            :key="pet.id"
            :value="pet.id"
          >
            {{ pet.name }} ({{ pet.type }} - {{ pet.breed }})
          </a-radio>
        </a-radio-group>
      </div>
      
      <div v-else-if="currentStep === 2" class="appointment-form">
        <a-form :model="appointmentForm" layout="vertical">
          <a-form-item label="预约时间" required>
            <a-date-picker
              v-model:value="appointmentForm.appointmentTime"
              show-time
              format="YYYY-MM-DD HH:mm"
            />
          </a-form-item>
          
          <a-form-item label="联系人" required>
            <a-input v-model:value="appointmentForm.contactName" />
          </a-form-item>
          
          <a-form-item label="联系电话" required>
            <a-input v-model:value="appointmentForm.contactPhone" />
          </a-form-item>
          
          <a-form-item label="服务地址">
            <a-input v-model:value="appointmentForm.address" />
          </a-form-item>
          
          <a-form-item label="备注">
            <a-textarea v-model:value="appointmentForm.remark" :rows="4" />
          </a-form-item>
        </a-form>
      </div>
      
      <div v-else-if="currentStep === 3" class="confirm-info">
        <a-descriptions bordered>
          <a-descriptions-item label="服务套餐">
            {{ selectedPackage?.name }}
          </a-descriptions-item>
          <a-descriptions-item label="价格">
            ¥{{ selectedPackage?.price }}
          </a-descriptions-item>
          <a-descriptions-item label="宠物">
            {{ selectedPet?.name }}
          </a-descriptions-item>
          <a-descriptions-item label="预约时间">
            {{ formatDateTime(appointmentForm.appointmentTime) }}
          </a-descriptions-item>
          <a-descriptions-item label="联系人">
            {{ appointmentForm.contactName }}
          </a-descriptions-item>
          <a-descriptions-item label="联系电话">
            {{ appointmentForm.contactPhone }}
          </a-descriptions-item>
        </a-descriptions>
      </div>
    </div>
    
    <div class="step-actions">
      <a-button v-if="currentStep > 0" @click="prevStep">
        上一步
      </a-button>
      <a-button
        v-if="currentStep < 3"
        type="primary"
        @click="nextStep"
      >
        下一步
      </a-button>
      <a-button
        v-else
        type="primary"
        :loading="submitting"
        @click="submitAppointment"
      >
        提交预约
      </a-button>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import { useRouter } from 'vue-router'
import { message } from 'ant-design-vue'
import { getPetList } from '@/api/pet'
import { getServiceList } from '@/api/service'
import { createAppointment } from '@/api/appointment'
import dayjs from 'dayjs'

const router = useRouter()

const currentStep = ref(0)
const packages = ref([])
const pets = ref([])
const selectedPackage = ref(null)
const selectedPetId = ref(null)
const submitting = ref(false)

const appointmentForm = ref({
  appointmentTime: null,
  contactName: '',
  contactPhone: '',
  address: '',
  remark: ''
})

const selectedPet = computed(() => {
  return pets.value.find(p => p.id === selectedPetId.value)
})

async function loadData() {
  const [pkgRes, petRes] = await Promise.all([
    getServiceList({ pageNum: 1, pageSize: 100 }),
    getPetList({ pageNum: 1, pageSize: 100 })
  ])
  packages.value = pkgRes.list
  pets.value = petRes.list
}

function selectPackage(pkg) {
  selectedPackage.value = pkg
}

function prevStep() {
  currentStep.value--
}

function nextStep() {
  if (currentStep.value === 0 && !selectedPackage.value) {
    message.warning('请选择服务套餐')
    return
  }
  if (currentStep.value === 1 && !selectedPetId.value) {
    message.warning('请选择宠物')
    return
  }
  currentStep.value++
}

async function submitAppointment() {
  submitting.value = true
  try {
    await createAppointment({
      packageId: selectedPackage.value.id,
      petId: selectedPetId.value,
      appointmentTime: appointmentForm.value.appointmentTime.toDate(),
      contactName: appointmentForm.value.contactName,
      contactPhone: appointmentForm.value.contactPhone,
      address: appointmentForm.value.address,
      remark: appointmentForm.value.remark
    })
    message.success('预约成功')
    router.push('/appointments')
  } catch (error) {
    message.error('预约失败')
  } finally {
    submitting.value = false
  }
}

function formatDateTime(date) {
  return dayjs(date).format('YYYY-MM-DD HH:mm')
}

loadData()
</script>
```

服务预约模块后端核心代码：

后端预约服务采用事务管理，确保数据的一致性。createAppointment方法首先验证宠物是否存在以及用户是否有权操作该宠物，然后验证服务套餐是否存在。验证通过后，生成唯一的订单号，创建预约记录并保存到数据库。订单号采用时间戳加随机UUID的方式生成，确保唯一性。预约创建成功后，调用processService.initProcess方法初始化服务流程，创建各个流程阶段的记录。整个方法使用@Transactional注解，确保操作的原子性，任何一步失败都会回滚事务。

```java
@Service
@RequiredArgsConstructor
public class AppointmentServiceImpl implements AppointmentService {
    
    private final AppointmentRepository appointmentRepository;
    private final PetRepository petRepository;
    private final ServicePackageRepository servicePackageRepository;
    private final ProcessService processService;
    
    @Override
    @Transactional
    public AppointmentResponse createAppointment(AppointmentRequest request, Long userId) {
        Pet pet = petRepository.findById(request.getPetId())
                .orElseThrow(() -> new BusinessException("宠物不存在"));
        
        if (!pet.getUserId().equals(userId)) {
            throw new BusinessException("无权操作此宠物");
        }
        
        ServicePackage servicePackage = servicePackageRepository.findById(request.getPackageId())
                .orElseThrow(() -> new BusinessException("服务套餐不存在"));
        
        Appointment appointment = new Appointment();
        appointment.setOrderNo(generateOrderNo());
        appointment.setUserId(userId);
        appointment.setPetId(request.getPetId());
        appointment.setPackageId(request.getPackageId());
        appointment.setAppointmentTime(request.getAppointmentTime());
        appointment.setContactName(request.getContactName());
        appointment.setContactPhone(request.getContactPhone());
        appointment.setAddress(request.getAddress());
        appointment.setRemark(request.getRemark());
        appointment.setStatus("pending");
        
        appointmentRepository.save(appointment);
        
        processService.initProcess(appointment.getId());
        
        return convertToResponse(appointment);
    }
    
    private String generateOrderNo() {
        String timestamp = LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyyMMddHHmmss"));
        String uuid = UUID.randomUUID().toString().replace("-", "").substring(0, 8);
        return "PM" + timestamp + uuid;
    }
}
```

## 4.3 流程追踪模块

流程追踪模块是本系统的特色功能之一，实现了服务全流程的可视化追踪。该模块通过时间线的形式展示服务的各个阶段，让用户能够实时了解服务进度，增强服务的透明度和可信度。流程追踪不仅满足了用户对服务过程的知情权，也体现了系统对宠物主人情感需求的关注，让主人在宠物离世后能够全程参与和了解告别仪式的每一个环节。

流程追踪模块将整个服务过程划分为七个阶段：确认预约、接送宠物、告别仪式、火化服务、骨灰处理、纪念品制作、服务完成。每个阶段都有明确的状态标识（待处理、进行中、已完成），通过不同的颜色和图标进行区分，让用户一目了然地了解当前进度。服务人员在每个阶段可以上传照片、视频和文字描述，记录服务的详细过程，这些信息会实时同步到用户的界面，让用户能够看到宠物接受的每一项服务。

系统采用WebSocket技术实现实时推送功能，当服务人员更新进度时，用户端会立即收到通知，无需刷新页面即可看到最新的进度信息。同时，系统会将每个阶段的时间节点、操作人员、服务内容等信息记录到数据库中，形成完整的服务档案，方便用户后续查看和追溯。在服务完成后，系统会自动生成服务证书，记录宠物的生命信息和服务详情，为宠物主人提供永久的纪念。

### 4.3.1 模块实现流程图

流程追踪模块实现用户对服务进度的实时查看，该流程从用户选择预约订单开始，系统自动查询该订单的服务流程记录，并以时间线的形式展示各个服务阶段。每个阶段显示当前状态、执行时间、操作描述以及相关的照片和视频等多媒体资料。用户可以点击查看详细的照片和视频，下载服务证书等文件。整个流程设计直观明了，让用户能够全面了解服务执行的每一个细节。其流程图如下所示。

```
开始
  │
  ▼
用户选择预约订单
  │
  ▼
查询服务流程记录
  │
  ▼
显示流程时间线
  │
  ├─── 确认预约 ──▶ 显示状态、时间、描述
  ├─── 接送宠物 ──▶ 显示状态、时间、照片
  ├─── 告别仪式 ──▶ 显示状态、时间、照片、视频
  ├─── 火化服务 ──▶ 显示状态、时间、照片、视频
  ├─── 骨灰处理 ──▶ 显示状态、时间、描述
  ├─── 纪念品制作 ──▶ 显示状态、时间、照片
  └─── 服务完成 ──▶ 显示状态、时间、证书
  │
  ▼
用户查看详细信息
  │
  ├─── 查看照片 ──▶ 点击放大查看
  ├─── 查看视频 ──▶ 在线播放
  └─── 下载证书 ──▶ 下载PDF文件
  │
  ▼
结束
```

图4-5 流程追踪流程图

### 4.3.2 模块界面展示

流程追踪页面采用垂直时间线布局，顶部显示订单基本信息，包括订单号、宠物名称和服务套餐名称。主体部分以时间线形式展示服务的七个阶段，每个阶段以节点形式呈现，通过不同颜色的圆点和标签区分状态：绿色表示已完成，蓝色表示进行中，灰色表示待处理。每个阶段节点下方显示执行时间、操作描述，部分阶段还展示服务人员上传的照片缩略图和视频播放器。照片以小图形式排列，点击可放大查看。整体设计清晰直观，用户可以一目了然地了解服务进度和详细情况。流程追踪页面如下图所示。

```
┌─────────────────────────────────────────────────────────────┐
│  服务进度                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  订单号：PM2024011512345678                                  │
│  宠物：小白 (萨摩耶)                                          │
│  服务：标准套餐                                               │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │  ● 确认预约                         [已完成]        │   │
│  │  │  2024-01-15 10:00                               │   │
│  │  │  预约已确认，等待服务人员上门                     │   │
│  │  │                                                  │   │
│  │  ● 接送宠物                         [已完成]        │   │
│  │  │  2024-01-15 14:00                               │   │
│  │  │  已接到宠物，正在前往服务中心                     │   │
│  │  │  [照片1] [照片2] [照片3]                         │   │
│  │  │                                                  │   │
│  │  ● 告别仪式                         [已完成]        │   │
│  │  │  2024-01-15 15:00                               │   │
│  │  │  告别仪式已完成                                  │   │
│  │  │  [照片1] [照片2] [视频1]                         │   │
│  │  │                                                  │   │
│  │  ● 火化服务                         [进行中]        │   │
│  │  │  2024-01-15 16:00                               │   │
│  │  │  正在进行火化服务                                │   │
│  │  │                                                  │   │
│  │  ○ 骨灰处理                         [待处理]        │   │
│  │  │                                                  │   │
│  │  ○ 纪念品制作                       [待处理]        │   │
│  │  │                                                  │   │
│  │  ○ 服务完成                         [待处理]        │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

图4-6 流程追踪页面图

### 4.3.3 核心代码展示

流程追踪模块的前端实现采用时间线组件展示服务进度，使用Ant Design Vue的Timeline组件。每个阶段对应一个Timeline.Item，通过颜色和标签展示不同状态。前端从后端API获取预约详情和流程记录，将流程阶段映射为中文显示名称。照片和视频以缩略图形式展示，点击可以预览或播放。页面加载时使用onMounted生命周期钩子获取数据，采用async/await处理异步请求。

流程追踪模块前端核心代码：

```vue
<template>
  <div class="process-tracking">
    <div class="process-header">
      <h2>服务进度</h2>
      <a-descriptions :column="3" bordered>
        <a-descriptions-item label="订单号">
          {{ appointment.orderNo }}
        </a-descriptions-item>
        <a-descriptions-item label="宠物">
          {{ appointment.petName }} ({{ appointment.petType }})
        </a-descriptions-item>
        <a-descriptions-item label="服务">
          {{ appointment.packageName }}
        </a-descriptions-item>
      </a-descriptions>
    </div>
    
    <div class="process-timeline">
      <a-timeline>
        <a-timeline-item
          v-for="(stage, index) in stages"
          :key="index"
          :color="getStageColor(stage.status)"
        >
          <div class="stage-header">
            <span class="stage-name">{{ stage.name }}</span>
            <a-tag :color="getStatusColor(stage.status)">
              {{ getStatusText(stage.status) }}
            </a-tag>
          </div>
          <div class="stage-time">{{ formatTime(stage.startTime) }}</div>
          <div v-if="stage.description" class="stage-desc">
            {{ stage.description }}
          </div>
          <div v-if="stage.photos?.length" class="stage-photos">
            <img
              v-for="(photo, idx) in stage.photos"
              :key="idx"
              :src="photo"
              class="stage-photo"
              @click="previewPhoto(photo)"
            />
          </div>
          <div v-if="stage.videos?.length" class="stage-videos">
            <video
              v-for="(video, idx) in stage.videos"
              :key="idx"
              :src="video"
              controls
              class="stage-video"
            />
          </div>
        </a-timeline-item>
      </a-timeline>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useRoute } from 'vue-router'
import { getAppointmentDetail } from '@/api/appointment'
import { getProcessByAppointment } from '@/api/process'
import dayjs from 'dayjs'

const route = useRoute()

const appointment = ref({})
const stages = ref([])

const stageNames = {
  confirmed: '确认预约',
  pickup: '接送宠物',
  farewell: '告别仪式',
  cremation: '火化服务',
  processing: '骨灰处理',
  memorial: '纪念品制作',
  completed: '服务完成'
}

async function loadData() {
  const id = route.params.id
  const [appointmentRes, processRes] = await Promise.all([
    getAppointmentDetail(id),
    getProcessByAppointment(id)
  ])
  appointment.value = appointmentRes
  stages.value = processRes.map(p => ({
    ...p,
    name: stageNames[p.stage]
  }))
}

function getStageColor(status) {
  const colors = {
    pending: 'gray',
    processing: 'blue',
    completed: 'green'
  }
  return colors[status] || 'gray'
}

function getStatusColor(status) {
  const colors = {
    pending: 'default',
    processing: 'processing',
    completed: 'success'
  }
  return colors[status] || 'default'
}

function getStatusText(status) {
  const texts = {
    pending: '待处理',
    processing: '进行中',
    completed: '已完成'
  }
  return texts[status] || '未知'
}

function formatTime(time) {
  return time ? dayjs(time).format('YYYY-MM-DD HH:mm:ss') : '-'
}

function previewPhoto(url) {
  window.open(url, '_blank')
}

onMounted(() => {
  loadData()
})
</script>
```

流程追踪模块后端核心代码：

后端流程服务实现了服务流程的初始化、更新和查询功能。initProcess方法在预约创建时被调用，为每个预约创建七个流程阶段的记录，初始状态为"待处理"。updateStageStatus方法用于更新流程阶段的状态，记录开始时间和结束时间，保存服务人员的操作记录、照片和视频。getProcessByAppointmentId方法查询指定预约的所有流程记录，按创建时间排序返回。流程记录中的照片和视频以JSON数组形式存储，查询时解析为List返回给前端。

```java
@Service
@RequiredArgsConstructor
public class ProcessServiceImpl implements ProcessService {
    
    private final ProcessRepository processRepository;
    private final AppointmentRepository appointmentRepository;
    
    @Override
    public void initProcess(Long appointmentId) {
        Appointment appointment = appointmentRepository.findById(appointmentId)
                .orElseThrow(() -> new BusinessException("预约不存在"));
        
        String[] stages = {"confirmed", "pickup", "farewell", "cremation", 
                          "processing", "memorial", "completed"};
        
        for (String stage : stages) {
            ProcessStage processStage = new ProcessStage();
            processStage.setAppointmentId(appointmentId);
            processStage.setStage(stage);
            processStage.setStatus("pending");
            processRepository.save(processStage);
        }
        
        updateStageStatus(appointmentId, "confirmed", "completed", null, "预约已确认");
    }
    
    @Override
    @Transactional
    public void updateStageStatus(Long appointmentId, String stage, String status, 
                                   Long operatorId, String description) {
        ProcessStage processStage = processRepository
                .findByAppointmentIdAndStage(appointmentId, stage)
                .orElseThrow(() -> new BusinessException("流程阶段不存在"));
        
        processStage.setStatus(status);
        processStage.setDescription(description);
        processStage.setOperatorId(operatorId);
        
        if ("processing".equals(status)) {
            processStage.setStartTime(LocalDateTime.now());
        } else if ("completed".equals(status)) {
            processStage.setEndTime(LocalDateTime.now());
        }
        
        processRepository.save(processStage);
    }
    
    @Override
    public List<ProcessResponse> getProcessByAppointmentId(Long appointmentId) {
        List<ProcessStage> stages = processRepository.findByAppointmentIdOrderByCreateTime(appointmentId);
        return stages.stream()
                .map(this::convertToResponse)
                .collect(Collectors.toList());
    }
    
    private ProcessResponse convertToResponse(ProcessStage stage) {
        ProcessResponse response = new ProcessResponse();
        response.setId(stage.getId());
        response.setStage(stage.getStage());
        response.setStatus(stage.getStatus());
        response.setDescription(stage.getDescription());
        response.setStartTime(stage.getStartTime());
        response.setEndTime(stage.getEndTime());
        response.setCreateTime(stage.getCreateTime());
        
        if (stage.getPhotos() != null) {
            response.setPhotos(JSON.parseArray(stage.getPhotos(), String.class));
        }
        if (stage.getVideos() != null) {
            response.setVideos(JSON.parseArray(stage.getVideos(), String.class));
        }
        
        return response;
    }
}
```

## 4.4 纪念相册模块

纪念相册模块是系统中最具情感关怀特色的功能模块，为宠物主人提供一个永久保存宠物记忆的空间。该模块允许用户为逝去的宠物创建专属的纪念相册，上传宠物生前的照片，撰写纪念文字，记录宠物与主人之间的美好回忆。纪念相册不仅是一个照片存储空间，更是一个情感寄托的场所，让宠物主人能够在这里缅怀和追思自己的爱宠。

纪念相册模块支持公开和私密两种访问模式。公开模式下，相册可以分享给亲友，让更多人能够缅怀逝去的宠物；私密模式下，只有相册创建者可以查看，保护用户的隐私。每个相册都配有留言板功能，允许访客在相册中发表追思留言，表达对宠物的怀念之情。留言支持游客模式，即使没有注册账号也可以留言，降低了使用门槛，方便更多人参与追思活动。

在技术实现上，纪念相册采用瀑布流布局展示照片，支持照片的懒加载和预览功能，提升用户体验。照片上传采用分片上传技术，支持大文件上传，上传过程中显示进度条，让用户了解上传进度。系统会对上传的照片进行压缩和缩略图生成，优化存储空间和加载速度。相册页面采用响应式设计，适配不同尺寸的屏幕，让用户在PC端和移动端都能获得良好的浏览体验。

### 4.4.1 模块实现流程图

纪念相册模块实现用户为逝去的宠物创建纪念相册，该流程从用户选择宠物开始，依次完成相册标题填写、相册描述编写、照片上传、访问权限设置等步骤，最后保存相册信息。相册创建成功后，用户可以查看相册页面，浏览照片，发表追思留言，以及将相册分享给亲友。整个流程设计简洁温馨，充分体现了对宠物主人情感需求的关怀。其流程图如下所示。

```
开始
  │
  ▼
用户选择宠物
  │
  ▼
创建纪念相册
  │
  ├─── 填写相册标题
  ├─── 填写相册描述
  ├─── 上传照片
  └─── 设置是否公开
  │
  ▼
保存相册信息
  │
  ▼
显示相册页面
  │
  ├─── 查看照片
  ├─── 发表留言
  └─── 分享相册
  │
  ▼
结束
```

图4-7 纪念相册流程图

### 4.4.2 模块界面展示

纪念相册页面采用温馨柔和的设计风格，页面顶部显示宠物名称和生命日期区间，配以淡雅的背景色，营造追思缅怀的氛围。主体部分分为两个区域：照片展示区和留言板区。照片展示区采用网格布局，照片以卡片形式均匀排列，每张照片下方显示简短的描述文字。鼠标悬停在照片上时，显示半透明的遮罩层和描述信息，增强交互体验。留言板区位于页面下方，采用卡片式设计，每条留言显示留言者姓名、留言内容和留言时间。留言输入框采用文本域形式，支持多行输入，底部配有发表按钮。整体设计温馨而不失庄重，为宠物主人提供一个安静缅怀的空间。纪念相册页面如下图所示。

```
┌─────────────────────────────────────────────────────────────┐
│  小白的纪念相册                                              │
│  2018-05-20 - 2024-01-15                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │
│  │ 照片 │ │ 照片 │ │ 照片 │ │ 照片 │ │ 照片 │ │ 照片 │   │
│  │  1  │ │  2  │ │  3  │ │  4  │ │  5  │ │  6  │   │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 留言板                                              │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 张三                                                │   │
│  │ 小白是一只很可爱的狗狗，愿你在汪星快乐！              │   │
│  │ 2024-01-16 10:30                                    │   │
│  │                                                     │   │
│  │ 李四                                                │   │
│  │ 看着小白从小到大，真的很舍不得。一路走好！            │   │
│  │ 2024-01-16 11:20                                    │   │
│  │                                                     │   │
│  │ [写下您的追思...]                                   │   │
│  │ [_____________________________________________]     │   │
│  │                                                     │   │
│  │                    [ 发表留言 ]                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

图4-8 纪念相册页面图

### 4.4.3 核心代码展示

纪念相册模块的前端实现采用网格布局展示照片，支持照片预览和留言功能。相册页面分为两个主要区域：照片展示区和留言板区。照片展示区使用CSS Grid布局，实现响应式的瀑布流效果。留言板区展示所有留言，支持分页加载。用户可以在留言框中输入追思内容，点击发表按钮提交留言。前端使用dayjs库格式化日期，提供友好的时间显示。页面加载时获取相册详情和留言列表，留言发表成功后刷新留言列表。

纪念相册模块前端核心代码：

```vue
<template>
  <div class="memorial-album">
    <div class="album-header">
      <h2>{{ petName }}的纪念相册</h2>
      <p class="memorial-dates">
        {{ formatDate(birthday) }} - {{ formatDate(passDate) }}
      </p>
    </div>
    
    <div class="album-photos">
      <div
        v-for="(photo, index) in photos"
        :key="index"
        class="photo-item"
        @click="handlePreview(index)"
      >
        <img :src="photo.url" :alt="photo.description" />
        <div class="photo-overlay">
          <p>{{ photo.description }}</p>
        </div>
      </div>
    </div>
    
    <div class="message-board">
      <h3>追思留言</h3>
      <div class="message-list">
        <div v-for="msg in messages" :key="msg.id" class="message-item">
          <div class="message-author">{{ msg.authorName }}</div>
          <div class="message-content">{{ msg.content }}</div>
          <div class="message-time">{{ formatDate(msg.createTime) }}</div>
        </div>
      </div>
      
      <a-textarea
        v-model:value="newMessage"
        placeholder="写下您的追思..."
        :rows="3"
        class="message-input"
      />
      <a-button type="primary" @click="handleSendMessage">
        发表留言
      </a-button>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useRoute } from 'vue-router'
import { message } from 'ant-design-vue'
import { getMemorialDetail, addMessage } from '@/api/memorial'
import dayjs from 'dayjs'

const route = useRoute()

const petName = ref('')
const birthday = ref('')
const passDate = ref('')
const photos = ref([])
const messages = ref([])
const newMessage = ref('')

async function loadMemorial() {
  const id = route.params.id
  const res = await getMemorialDetail(id)
  petName.value = res.petName
  birthday.value = res.birthday
  passDate.value = res.passDate
  photos.value = res.photos || []
  messages.value = res.messages || []
}

function formatDate(date) {
  return dayjs(date).format('YYYY年MM月DD日')
}

function handlePreview(index) {
  console.log('Preview photo:', index)
}

async function handleSendMessage() {
  if (!newMessage.value.trim()) {
    message.warning('请输入留言内容')
    return
  }
  
  try {
    await addMessage({
      albumId: route.params.id,
      content: newMessage.value.trim()
    })
    message.success('留言成功')
    newMessage.value = ''
    loadMemorial()
  } catch (error) {
    message.error('留言失败')
  }
}

onMounted(() => {
  loadMemorial()
})
</script>
```

纪念相册模块后端核心代码：

后端纪念相册服务实现了相册的创建、查询和留言功能。createMemorial方法验证用户是否有权为指定宠物创建相册，然后创建相册记录并保存。getMemorialDetail方法查询相册详情，同时增加浏览次数，并查询相册关联的宠物信息和留言列表。addMessage方法允许用户或游客在相册中发表留言，留言内容保存到message表，关联相册ID和用户信息。相册和留言采用软删除机制，删除时只更新deleted字段，不真正删除记录，保留数据完整性。

```java
@Service
@RequiredArgsConstructor
public class MemorialServiceImpl implements MemorialService {
    
    private final MemorialRepository memorialRepository;
    private final MessageRepository messageRepository;
    private final PetRepository petRepository;
    
    @Override
    @Transactional
    public MemorialResponse createMemorial(MemorialRequest request, Long userId) {
        Pet pet = petRepository.findById(request.getPetId())
                .orElseThrow(() -> new BusinessException("宠物不存在"));
        
        if (!pet.getUserId().equals(userId)) {
            throw new BusinessException("无权操作此宠物");
        }
        
        MemorialAlbum album = new MemorialAlbum();
        album.setPetId(request.getPetId());
        album.setUserId(userId);
        album.setTitle(request.getTitle());
        album.setDescription(request.getDescription());
        album.setPhotos(JSON.toJSONString(request.getPhotos()));
        album.setIsPublic(request.getIsPublic());
        album.setViewCount(0);
        
        memorialRepository.save(album);
        
        return convertToResponse(album);
    }
    
    @Override
    public MemorialResponse getMemorialDetail(Long id) {
        MemorialAlbum album = memorialRepository.findById(id)
                .orElseThrow(() -> new BusinessException("纪念相册不存在"));
        
        album.setViewCount(album.getViewCount() + 1);
        memorialRepository.save(album);
        
        return convertToResponse(album);
    }
    
    @Override
    @Transactional
    public void addMessage(Long albumId, String content, Long userId, String authorName) {
        MemorialAlbum album = memorialRepository.findById(albumId)
                .orElseThrow(() -> new BusinessException("纪念相册不存在"));
        
        Message message = new Message();
        message.setAlbumId(albumId);
        message.setUserId(userId);
        message.setAuthorName(authorName);
        message.setContent(content);
        
        messageRepository.save(message);
    }
    
    private MemorialResponse convertToResponse(MemorialAlbum album) {
        MemorialResponse response = new MemorialResponse();
        response.setId(album.getId());
        response.setTitle(album.getTitle());
        response.setDescription(album.getDescription());
        response.setIsPublic(album.getIsPublic());
        response.setViewCount(album.getViewCount());
        response.setCreateTime(album.getCreateTime());
        
        if (album.getPhotos() != null) {
            response.setPhotos(JSON.parseArray(album.getPhotos(), String.class));
        }
        
        Pet pet = petRepository.findById(album.getPetId()).orElse(null);
        if (pet != null) {
            response.setPetName(pet.getName());
            response.setBirthday(pet.getBirthday());
            response.setPassDate(pet.getPassDate());
        }
        
        List<Message> messages = messageRepository.findByAlbumIdOrderByCreateTimeDesc(album.getId());
        response.setMessages(messages.stream()
                .map(this::convertMessageToResponse)
                .collect(Collectors.toList()));
        
        return response;
    }
}
```

## 4.5 管理员管理模块

管理员管理模块是系统的后台管理核心，为管理员提供用户管理、服务管理、预约管理、统计分析等功能。该模块采用权限控制机制，只有具有管理员角色的用户才能访问，确保了系统的安全性。管理员通过该模块可以对整个系统的运行进行监控和管理，维护系统的正常运转，为用户提供优质的服务。

管理员管理模块包括多个子功能：用户管理功能允许管理员查看所有用户信息，对违规用户进行禁用或启用操作，管理用户的角色和权限；服务管理功能允许管理员添加、编辑、删除服务套餐，设置套餐价格、描述和状态，管理服务类型；预约管理功能允许管理员查看所有预约订单，对预约进行审核、分配服务人员、更新状态等操作；统计分析功能提供数据可视化展示，包括服务统计、用户统计、收入统计等，帮助管理员了解系统运营情况，做出决策支持。

在技术实现上，管理员管理模块采用Ant Design Vue的Table组件展示数据列表，支持分页、排序、筛选等功能。数据统计使用ECharts图表库，以柱状图、折线图、饼图等形式展示数据，直观易懂。后端采用Spring Data JPA进行数据查询，支持复杂的条件查询和分页查询，使用事务管理确保数据一致性。

### 4.5.1 模块实现流程图

管理员管理模块实现用户管理、服务管理、预约管理等功能，以用户管理为例，其流程图如下所示。

```
开始
  │
  ▼
管理员登录系统
  │
  ▼
进入管理后台
  │
  ▼
选择管理功能
  │
  ├─── 用户管理 ──▶ 查看用户列表 ──▶ 编辑/禁用/启用用户
  ├─── 服务管理 ──▶ 管理服务套餐 ──▶ 添加/编辑/删除套餐
  ├─── 预约管理 ──▶ 查看预约列表 ──▶ 审核/分配/更新状态
  └─── 统计分析 ──▶ 查看数据报表 ──▶ 导出数据
  │
  ▼
保存操作结果
  │
  ▼
结束
```

图4-9 管理员管理流程图

### 4.5.2 模块界面展示

管理员后台页面如下图所示。

```
┌─────────────────────────────────────────────────────────────┐
│  管理后台                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 用户管理  │  │ 服务管理  │  │ 预约管理  │  │ 统计分析  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│                                                             │
│  用户列表                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 用户ID │ 用户名 │ 手机号 │ 角色 │ 状态 │ 操作       │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 001   │ 张三   │ 138*** │ 用户 │ 启用 │ [编辑][禁用]│   │
│  │ 002   │ 李四   │ 139*** │ 服务 │ 启用 │ [编辑][禁用]│   │
│  │ 003   │ 王五   │ 137*** │ 用户 │ 禁用 │ [编辑][启用]│   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  [上一页] 1 2 3 4 5 [下一页]  共50条记录                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

图4-10 管理员后台页面图

管理员后台页面采用左侧菜单、右侧内容的经典布局，顶部显示系统名称和管理员信息。左侧菜单以列表形式展示各个管理功能，点击菜单项切换右侧内容区域。用户列表页面以表格形式展示用户信息，支持按用户名、手机号、角色等条件筛选，支持分页显示。每行数据的操作列提供编辑和禁用/启用按钮，方便管理员快速操作。页面底部显示分页控件和总记录数，方便管理员了解数据量。

### 4.5.3 核心代码展示

管理员管理模块的前端实现采用Ant Design Vue的Layout组件构建页面布局，使用Menu组件实现左侧导航菜单，使用Table组件展示数据列表。表格支持分页、排序、筛选等功能，通过rowKey属性指定行的唯一标识，通过columns属性定义表格列的配置。操作列使用slot插槽自定义渲染，提供编辑、删除等操作按钮。数据加载使用async/await处理异步请求，通过loading状态控制表格的加载状态。

管理员管理模块前端核心代码：

```vue
<template>
  <div class="admin-layout">
    <a-layout>
      <a-layout-sider v-model:collapsed="collapsed" collapsible>
        <div class="logo">管理后台</div>
        <a-menu
          v-model:selectedKeys="selectedKeys"
          theme="dark"
          mode="inline"
        >
          <a-menu-item key="users">
            <UserOutlined />
            <span>用户管理</span>
          </a-menu-item>
          <a-menu-item key="services">
            <AppstoreOutlined />
            <span>服务管理</span>
          </a-menu-item>
          <a-menu-item key="appointments">
            <CalendarOutlined />
            <span>预约管理</span>
          </a-menu-item>
          <a-menu-item key="statistics">
            <BarChartOutlined />
            <span>统计分析</span>
          </a-menu-item>
        </a-menu>
      </a-layout-sider>
      
      <a-layout>
        <a-layout-header>
          <a-dropdown>
            <a class="ant-dropdown-link">
              {{ adminInfo.username }} <DownOutlined />
            </a>
            <template #overlay>
              <a-menu>
                <a-menu-item @click="logout">退出登录</a-menu-item>
              </a-menu>
            </template>
          </a-dropdown>
        </a-layout-header>
        
        <a-layout-content>
          <div class="content">
            <a-table
              :columns="columns"
              :data-source="data"
              :loading="loading"
              :pagination="pagination"
              rowKey="id"
            >
              <template #action="{ record }">
                <a-button type="link" @click="handleEdit(record)">
                  编辑
                </a-button>
                <a-button
                  type="link"
                  @click="handleToggleStatus(record)"
                >
                  {{ record.status === 1 ? '禁用' : '启用' }}
                </a-button>
              </template>
            </a-table>
          </div>
        </a-layout-content>
      </a-layout>
    </a-layout>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { message } from 'ant-design-vue'
import {
  UserOutlined,
  AppstoreOutlined,
  CalendarOutlined,
  BarChartOutlined,
  DownOutlined
} from '@ant-design/icons-vue'
import { getUserList, updateUserStatus } from '@/api/admin'

const collapsed = ref(false)
const selectedKeys = ref(['users'])
const adminInfo = ref({})
const data = ref([])
const loading = ref(false)

const columns = [
  { title: '用户ID', dataIndex: 'id', key: 'id' },
  { title: '用户名', dataIndex: 'username', key: 'username' },
  { title: '手机号', dataIndex: 'phone', key: 'phone' },
  { title: '角色', dataIndex: 'role', key: 'role' },
  { title: '状态', dataIndex: 'status', key: 'status' },
  { title: '操作', key: 'action', slots: { customRender: 'action' } }
]

const pagination = {
  current: 1,
  pageSize: 10,
  total: 0,
  showSizeChanger: true,
  showTotal: (total) => `共 ${total} 条记录`
}

async function loadData() {
  loading.value = true
  try {
    const res = await getUserList({
      pageNum: pagination.current,
      pageSize: pagination.pageSize
    })
    data.value = res.list
    pagination.total = res.total
  } catch (error) {
    message.error('加载数据失败')
  } finally {
    loading.value = false
  }
}

function handleEdit(record) {
  console.log('Edit user:', record)
}

async function handleToggleStatus(record) {
  try {
    const newStatus = record.status === 1 ? 0 : 1
    await updateUserStatus(record.id, newStatus)
    message.success('操作成功')
    loadData()
  } catch (error) {
    message.error('操作失败')
  }
}

function logout() {
  console.log('Logout')
}

onMounted(() => {
  loadData()
})
</script>
```

管理员管理模块后端核心代码：

后端管理员服务实现了用户管理、服务管理、预约管理等功能。getUserList方法查询用户列表，支持按用户名、手机号、角色等条件筛选，支持分页查询。updateUserStatus方法更新用户状态，将用户状态在启用和禁用之间切换。updateUserRole方法更新用户角色，支持将用户设置为普通用户、服务人员或管理员。所有管理操作都记录操作日志，方便追溯。管理服务采用权限控制，只有具有管理员角色的用户才能调用相关接口。

```java
@Service
@RequiredArgsConstructor
public class AdminServiceImpl implements AdminService {
    
    private final UserRepository userRepository;
    private final ServicePackageRepository servicePackageRepository;
    private final AppointmentRepository appointmentRepository;
    
    @Override
    public PageResult<UserDTO> getUserList(UserQueryRequest request) {
        Specification<User> spec = (root, query, cb) -> {
            List<Predicate> predicates = new ArrayList<>();
            
            if (StringUtils.hasText(request.getUsername())) {
                predicates.add(cb.like(root.get("username"), 
                    "%" + request.getUsername() + "%"));
            }
            
            if (StringUtils.hasText(request.getPhone())) {
                predicates.add(cb.like(root.get("phone"), 
                    "%" + request.getPhone() + "%"));
            }
            
            if (StringUtils.hasText(request.getRole())) {
                predicates.add(cb.equal(root.get("role"), request.getRole()));
            }
            
            return cb.and(predicates.toArray(new Predicate[0]));
        };
        
        Pageable pageable = PageRequest.of(
            request.getPageNum() - 1, 
            request.getPageSize(),
            Sort.by("createTime").descending()
        );
        
        Page<User> page = userRepository.findAll(spec, pageable);
        
        return new PageResult<>(
            page.map(this::convertToDTO).getContent(),
            page.getTotalElements(),
            request.getPageNum(),
            request.getPageSize()
        );
    }
    
    @Override
    @Transactional
    public void updateUserStatus(Long userId, Integer status) {
        User user = userRepository.findById(userId)
                .orElseThrow(() -> new BusinessException("用户不存在"));
        
        user.setStatus(status);
        userRepository.save(user);
    }
    
    @Override
    @Transactional
    public void updateUserRole(Long userId, String role) {
        User user = userRepository.findById(userId)
                .orElseThrow(() -> new BusinessException("用户不存在"));
        
        user.setRole(role);
        userRepository.save(user);
    }
    
    private UserDTO convertToDTO(User user) {
        UserDTO dto = new UserDTO();
        dto.setId(user.getId());
        dto.setUsername(user.getUsername());
        dto.setPhone(user.getPhone());
        dto.setEmail(user.getEmail());
        dto.setRealName(user.getRealName());
        dto.setAvatar(user.getAvatar());
        dto.setRole(user.getRole());
        dto.setStatus(user.getStatus());
        dto.setCreateTime(user.getCreateTime());
        return dto;
    }
}
```

## 4.6 文件管理模块

文件管理模块是系统的基础支撑模块，负责处理系统中所有文件的上传、下载、存储和管理。该模块支持多种文件类型，包括图片（jpg、png、gif、webp）、视频（mp4、webm）、文档（pdf）等，满足宠物殡葬服务中照片、视频、证书等多种文件的处理需求。模块采用分片上传、断点续传等技术，支持大文件上传，提高用户体验。

文件管理模块的核心功能包括：文件上传功能支持单文件和多文件上传，上传过程中显示进度条，支持取消上传操作；文件下载功能支持断点续传，大文件下载时可以暂停和恢复；文件预览功能支持图片预览、视频播放、PDF在线查看等；文件管理功能支持文件的删除、移动、重命名等操作。系统会对上传的文件进行安全检查，包括文件类型验证、文件大小限制、文件内容检测等，防止恶意文件上传。

在技术实现上，文件上传采用MultipartFile接口处理，前端使用FormData对象封装文件数据，通过axios发送multipart/form-data请求。后端接收文件后，进行类型和大小验证，生成唯一的文件名（使用UUID），按日期分目录存储，将文件信息保存到数据库。文件存储路径采用相对路径，格式为：/upload/{模块名}/{年}/{月}/{日}/{文件名}，方便管理和备份。系统支持本地存储和云存储两种方式，可根据需求灵活切换。

### 4.6.1 模块实现流程图

文件管理模块实现文件的上传、下载和管理，其流程图如下所示。

```
开始
  │
  ▼
用户选择文件
  │
  ▼
验证文件类型和大小
  │
  ├─── 不通过 ──▶ 提示错误信息 ──▶ 返回选择文件
  │
  通过
  │
  ▼
生成唯一文件名
  │
  ▼
上传文件到服务器
  │
  ├─── 上传中 ──▶ 显示进度条
  │
  上传完成
  │
  ▼
保存文件信息到数据库
  │
  ▼
返回文件访问路径
  │
  ▼
结束
```

图4-11 文件管理流程图

### 4.6.2 模块界面展示

文件上传页面如下图所示。

```
┌─────────────────────────────────────────────────────────────┐
│  文件上传                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │         点击或拖拽文件到此区域上传                   │   │
│  │                                                     │   │
│  │         支持格式：jpg、png、gif、mp4、pdf           │   │
│  │         单个文件不超过50MB                          │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  已上传文件列表                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 文件名       │ 大小   │ 进度   │ 状态   │ 操作     │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ photo1.jpg  │ 2.5MB  │ 100%  │ 完成   │ [查看][删除]│   │
│  │ video1.mp4  │ 15MB   │ 60%   │ 上传中 │ [取消]     │   │
│  │ cert.pdf    │ 1.2MB  │ 0%    │ 等待   │ [删除]     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

图4-12 文件上传页面图

文件上传页面采用拖拽上传和点击上传两种方式，用户可以将文件直接拖拽到上传区域，也可以点击上传区域选择文件。上传区域显示支持的文件格式和大小限制，帮助用户了解上传要求。已上传文件列表以表格形式展示，显示文件名、文件大小、上传进度、上传状态等信息。上传中的文件显示进度条，用户可以取消上传。已完成的文件提供查看和删除操作，方便用户管理文件。

### 4.6.3 核心代码展示

文件管理模块的前端实现采用Ant Design Vue的Upload组件，支持拖拽上传、多文件上传、进度显示等功能。Upload组件通过action属性指定上传地址，通过headers属性设置请求头（携带JWT令牌），通过beforeUpload方法进行上传前的验证，通过onChange方法监听上传状态变化。文件列表使用Table组件展示，通过progress属性显示上传进度。图片文件支持预览功能，使用Modal组件显示大图。

文件管理模块前端核心代码：

```vue
<template>
  <div class="file-upload">
    <a-upload-dragger
      :action="uploadUrl"
      :headers="headers"
      :multiple="true"
      :before-upload="beforeUpload"
      @change="handleChange"
    >
      <p class="ant-upload-drag-icon">
        <InboxOutlined />
      </p>
      <p class="ant-upload-text">点击或拖拽文件到此区域上传</p>
      <p class="ant-upload-hint">
        支持格式：jpg、png、gif、mp4、pdf<br>
        单个文件不超过50MB
      </p>
    </a-upload-dragger>
    
    <a-table
      :columns="columns"
      :data-source="fileList"
      :pagination="false"
      rowKey="uid"
      class="file-list"
    >
      <template #progress="{ record }">
        <a-progress
          v-if="record.status === 'uploading'"
          :percent="record.percent"
        />
        <span v-else>{{ record.status === 'done' ? '完成' : '等待' }}</span>
      </template>
      
      <template #action="{ record }">
        <a-button
          v-if="record.status === 'done'"
          type="link"
          @click="handlePreview(record)"
        >
          查看
        </a-button>
        <a-button
          v-if="record.status === 'uploading'"
          type="link"
          @click="handleCancel(record)"
        >
          取消
        </a-button>
        <a-button
          type="link"
          danger
          @click="handleDelete(record)"
        >
          删除
        </a-button>
      </template>
    </a-table>
    
    <a-modal
      v-model:visible="previewVisible"
      :footer="null"
      @cancel="previewVisible = false"
    >
      <img :src="previewUrl" style="width: 100%" />
    </a-modal>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { message } from 'ant-design-vue'
import { InboxOutlined } from '@ant-design/icons-vue'
import { getToken } from '@/utils/auth'

const uploadUrl = import.meta.env.VITE_API_BASE_URL + '/v1/files/upload'
const headers = {
  Authorization: `Bearer ${getToken()}`
}

const fileList = ref([])
const previewVisible = ref(false)
const previewUrl = ref('')

const columns = [
  { title: '文件名', dataIndex: 'name', key: 'name' },
  { title: '大小', dataIndex: 'size', key: 'size' },
  { title: '进度', key: 'progress', slots: { customRender: 'progress' } },
  { title: '状态', dataIndex: 'status', key: 'status' },
  { title: '操作', key: 'action', slots: { customRender: 'action' } }
]

function beforeUpload(file) {
  const allowedTypes = ['image/jpeg', 'image/png', 'image/gif', 
                        'video/mp4', 'application/pdf']
  const isAllowed = allowedTypes.includes(file.type)
  
  if (!isAllowed) {
    message.error('不支持的文件类型')
    return false
  }
  
  const isLt50M = file.size / 1024 / 1024 < 50
  if (!isLt50M) {
    message.error('文件大小不能超过50MB')
    return false
  }
  
  return true
}

function handleChange(info) {
  const { file, fileList: newFileList } = info
  
  if (file.status === 'done') {
    message.success(`${file.name} 上传成功`)
  } else if (file.status === 'error') {
    message.error(`${file.name} 上传失败`)
  }
  
  fileList.value = newFileList
}

function handlePreview(record) {
  if (record.response && record.response.data) {
    previewUrl.value = record.response.data.url
    previewVisible.value = true
  }
}

function handleCancel(record) {
  console.log('Cancel upload:', record)
}

function handleDelete(record) {
  const index = fileList.value.findIndex(f => f.uid === record.uid)
  if (index > -1) {
    fileList.value.splice(index, 1)
  }
}
</script>
```

文件管理模块后端核心代码：

后端文件服务实现了文件的上传、下载、删除等功能。uploadFile方法接收MultipartFile对象，首先验证文件类型和大小，然后生成唯一的文件名（使用UUID），按日期创建存储目录，将文件保存到磁盘，最后将文件信息保存到数据库。文件存储路径采用相对路径，格式为：/upload/{模块名}/{年}/{月}/{日}/{文件名}。downloadFile方法根据文件路径读取文件，返回文件流供前端下载。deleteFile方法删除磁盘上的文件，同时删除数据库中的文件记录。文件服务支持多种存储方式，包括本地存储和云存储（如阿里云OSS、腾讯云COS等），通过配置文件切换。

```java
@Service
@RequiredArgsConstructor
public class FileServiceImpl implements FileService {
    
    @Value("${file.upload.path}")
    private String uploadPath;
    
    @Value("${file.upload.allowed-types}")
    private String allowedTypes;
    
    @Value("${file.upload.max-size}")
    private Long maxSize;
    
    private final FileRecordRepository fileRecordRepository;
    
    @Override
    public String uploadFile(MultipartFile file, String entityType, Long entityId, Long uploaderId) {
        validateFile(file);
        
        String originalFilename = file.getOriginalFilename();
        String extension = FilenameUtils.getExtension(originalFilename);
        String storedName = UUID.randomUUID().toString() + "." + extension;
        
        String datePath = LocalDate.now().format(DateTimeFormatter.ofPattern("yyyy/MM/dd"));
        String relativePath = String.format("/%s/%s/%s", entityType, datePath, storedName);
        String absolutePath = uploadPath + relativePath;
        
        try {
            Path path = Paths.get(absolutePath);
            Files.createDirectories(path.getParent());
            file.transferTo(path.toFile());
            
            FileRecord fileRecord = new FileRecord();
            fileRecord.setOriginalName(originalFilename);
            fileRecord.setStoredName(storedName);
            fileRecord.setFilePath(relativePath);
            fileRecord.setFileSize(file.getSize());
            fileRecord.setFileType(file.getContentType());
            fileRecord.setEntityType(entityType);
            fileRecord.setEntityId(entityId);
            fileRecord.setUploaderId(uploaderId);
            fileRecordRepository.save(fileRecord);
            
            return relativePath;
        } catch (IOException e) {
            throw new BusinessException("文件上传失败: " + e.getMessage());
        }
    }
    
    @Override
    public void deleteFile(String filePath) {
        try {
            Path path = Paths.get(uploadPath + filePath);
            Files.deleteIfExists(path);
            fileRecordRepository.deleteByFilePath(filePath);
        } catch (IOException e) {
            throw new BusinessException("文件删除失败: " + e.getMessage());
        }
    }
    
    private void validateFile(MultipartFile file) {
        if (file.isEmpty()) {
            throw new BusinessException("上传文件不能为空");
        }
        
        if (file.getSize() > maxSize) {
            throw new BusinessException("文件大小超过限制");
        }
        
        String extension = FilenameUtils.getExtension(file.getOriginalFilename());
        List<String> allowedTypeList = Arrays.asList(allowedTypes.split(","));
        if (!allowedTypeList.contains(extension.toLowerCase())) {
            throw new BusinessException("不支持的文件类型");
        }
    }
}
```

---

# 5 系统测试

## 5.1 系统测试概述

在整个软件的系统设计完成之后，就需要对软件进行一个完整的系统的测试，该测试的主要目的在于发现整个软件系统中的未知错误。系统测试是在整个软件发开中期中最重要的一步，测试的结果关系到软件能否上线到市场中，给用户提供安全可靠的系统软件，同时验证软件是否满足软件设计方案中所规划设计的功能需求。在测试阶段应正确设计测试用例，并按照这些设计的用力来进行测试，来发现问题及完善系统。

系统测试的主要目标包括：
（1）验证系统功能是否符合需求规格说明书的要求；
（2）发现系统中存在的缺陷和错误；
（3）评估系统的性能和稳定性；
（4）验证系统的安全性和可靠性；
（5）为系统上线提供质量保证。

## 5.2 系统测试方法

用来测试软件的方法有很多种，大体分为两种测试方法，其中一种是静态测试的方式，另一种则是动态测试的方法。而其中动态测试方法分为两种，一种是白盒测试的方法，另一种是黑盒测试的方法。该系统便是使用黑盒测试的方法。

黑盒测试也被人们称为功能测试。程序接口只检查其程序的功能是否可以正确使用，以及其输入的数据和输出的信息是不是可以根据其规范能够正确地接收，并且可以维护其外部信息的完整性。黑盒测试主要从以下几个方面进行：

（1）功能测试：验证每个功能是否按照需求规格说明书的要求正常工作，包括正常情况和异常情况的处理。

（2）界面测试：验证用户界面是否符合设计要求，界面元素是否正确显示，交互是否流畅。

（3）性能测试：验证系统在规定负载下的响应时间、吞吐量等性能指标是否满足要求。

（4）安全测试：验证系统的安全机制是否有效，包括身份认证、权限控制、数据加密等。

（5）兼容性测试：验证系统在不同浏览器、不同设备上的兼容性。

## 5.3 系统测试用例

### 5.3.1 测试项目

测试项目如表5-1所示。

表5-1 测试项目表

| 功能编号 | 测试项编号 | 测试内容 | 测试优先级 |
|---------|-----------|----------|-----------|
| 0001 | A0001 | 用户注册功能 | 高 |
| 0002 | A0002 | 用户登录功能 | 高 |
| 0003 | A0003 | 宠物信息管理 | 高 |
| 0004 | B0001 | 服务预约功能 | 高 |
| 0005 | B0002 | 预约管理功能 | 高 |
| 0006 | B0003 | 流程追踪功能 | 高 |
| 0007 | C0001 | 纪念相册功能 | 中 |
| 0008 | C0002 | 留言功能 | 中 |
| 0009 | D0001 | 文件上传功能 | 中 |
| 0010 | D0002 | 支付功能 | 高 |

### 5.3.2 测试需求

测试需求如表5-2所示。

表5-2 测试项目需求表

| 序号 | 测试功能 | 测试优先级 |
|------|----------|-----------|
| A0001 | 用户注册 | 高 |
| A0002 | 用户登录 | 高 |
| A0003 | 用户信息修改 | 中 |
| A0004 | 宠物信息添加 | 高 |
| A0005 | 宠物信息修改 | 中 |
| A0006 | 宠物信息删除 | 中 |
| B0001 | 服务套餐浏览 | 高 |
| B0002 | 服务预约提交 | 高 |
| B0003 | 预约状态查询 | 高 |
| B0004 | 预约取消 | 中 |
| C0001 | 纪念相册创建 | 中 |
| C0002 | 纪念相册查看 | 中 |
| C0003 | 留言发表 | 中 |
| D0001 | 文件上传 | 中 |
| D0002 | 文件下载 | 中 |

### 5.3.3 测试用例

测试用例如表5-3所示。

表5-3 测试项目用例表

| 测试需求 | 宠物殡葬管理系统 |
|---------|-----------------|
| 描述 | 系统所有功能的测试 |
| 优先级 | 高 |
| 预置条件 | 管理员登录平台系统（账号：admin 密码：admin123） |
| 测试时间 | 2024.01.15 | 测试人员 | 开发人员 |
| 测试用例序号 | 输入条件 | 操作步骤 | 预期输出 | 测试结果 |
| A0001 | 注册新用户 | 填写用户名、密码、手机号后点击注册按钮 | 注册成功，跳转到登录页面 | 测试通过 |
| A0002 | 用户登录 | 输入正确的用户名和密码后点击登录按钮 | 登录成功，跳转到首页 | 测试通过 |
| A0003 | 用户信息修改 | 修改个人信息后点击保存按钮 | 修改成功，显示更新后的信息 | 测试通过 |
| A0004 | 宠物信息添加 | 填写宠物信息后点击添加按钮 | 添加成功，显示在宠物列表中 | 测试通过 |
| A0005 | 宠物信息修改 | 修改宠物信息后点击保存按钮 | 修改成功，显示更新后的信息 | 测试通过 |
| A0006 | 宠物信息删除 | 点击删除按钮后确认删除 | 删除成功，从列表中移除 | 测试通过 |
| B0001 | 服务套餐浏览 | 访问服务套餐页面 | 显示所有上架的服务套餐 | 测试通过 |
| B0002 | 服务预约提交 | 选择套餐、宠物，填写预约信息后提交 | 预约成功，生成预约订单 | 测试通过 |
| B0003 | 预约状态查询 | 查看预约详情 | 显示预约状态和服务进度 | 测试通过 |
| B0004 | 预约取消 | 点击取消预约按钮后确认 | 取消成功，预约状态更新为已取消 | 测试通过 |
| C0001 | 纪念相册创建 | 填写相册信息，上传照片后提交 | 创建成功，显示相册页面 | 测试通过 |
| C0002 | 纪念相册查看 | 访问纪念相册页面 | 显示相册内容和留言 | 测试通过 |
| C0003 | 留言发表 | 输入留言内容后点击发表按钮 | 发表成功，显示在留言列表中 | 测试通过 |
| D0001 | 文件上传 | 选择文件后点击上传按钮 | 上传成功，返回文件URL | 测试通过 |
| D0002 | 文件下载 | 点击文件链接 | 文件下载成功 | 测试通过 |

---

# 6 结论与展望

经过段时间宠物殡葬管理系统的设计及论文的书写，至此已基本完成任务书中的任务及要求。这期间，让我有很大的感触，这次设计是我从大学生走向未来工程师重要的一步。从刚开始选题、开题到最后系统需求分析、完成设计，在这个过程中每一次的资料查找、老师指导、与同学的交流、修订需求和设计都是对自己能力的检验和充实。在这个过程中，我受益颇丰，对项目开发有了更深刻的了解，尤其是系统的设计，深刻体会到软件工程项目设计的过程。

通过本次毕业设计，我完成了以下工作：

（1）完成了宠物殡葬管理系统的需求分析，包括可行性分析、业务流分析、数据流分析等，明确了系统的功能需求和非功能需求。

（2）完成了系统的概要设计，包括系统架构设计、数据库设计、接口设计等，为系统的详细设计奠定了基础。

（3）完成了系统的详细设计，实现了用户管理、宠物管理、服务预约、流程追踪、纪念相册等核心功能模块。

（4）完成了系统的测试工作，通过黑盒测试方法验证了系统功能的正确性和稳定性。

（5）撰写了完整的毕业论文，对系统的设计过程和实现方法进行了详细的阐述。

本系统具有以下特点和创新之处：

（1）技术先进：采用前后端分离架构，前端使用Vue 3 + Vite + TypeScript技术栈，后端使用Spring Boot 3 + Hibernate技术栈，技术选型先进，开发效率高。

（2）功能完善：系统涵盖了用户管理、宠物管理、服务预约、流程追踪、纪念相册等核心功能，满足了宠物殡葬服务的基本需求。

（3）流程透明：通过流程追踪功能，用户可以实时查看服务进度，了解每个阶段的执行情况，提高了服务的透明度。

（4）情感化设计：系统融入了情感关怀元素，如纪念相册、追思留言等功能，体现了对生命的尊重和对人性的关怀。

（5）安全可靠：采用JWT身份认证、密码加密、权限控制等安全措施，保障了系统的安全性。

当然，在这个过程中我遇到了很多困难，每次的困难都在告诉我，我的专业知识和综合应用能力是不足的。可以说这次设计是对我最大的检阅，让我认识到虽然即将毕业，但是我的求学路还很长，正所谓"学无止境"，以后更应该努力学习，争取能够为社会的发展添砖加瓦。

系统还存在一些不足之处，需要在未来的工作中进一步完善：

（1）支付功能：目前系统只设计了支付接口，尚未完成与第三方支付平台的对接，需要进一步开发支付功能。

（2）移动端适配：目前系统主要针对PC端进行了设计，移动端适配还不够完善，需要进一步优化移动端的用户体验。

（3）性能优化：系统在大数据量情况下的性能表现还需要进一步测试和优化，如数据库查询优化、缓存机制等。

（4）功能扩展：系统可以进一步扩展功能，如在线客服、评价系统、推荐系统等，提升用户体验。

（5）数据分析：可以增加数据分析功能，如用户行为分析、服务趋势分析等，为运营决策提供支持。

展望未来，随着宠物经济的持续发展和人们生活水平的提高，宠物殡葬服务市场将迎来更大的发展机遇。本系统将在实践中不断完善和优化，为宠物主人提供更加便捷、温馨、专业的服务体验。同时，我也将继续学习和研究相关技术，不断提升自己的专业能力，为宠物殡葬行业的数字化转型贡献自己的力量。
